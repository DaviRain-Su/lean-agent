# AI Parity Ledger

This file tracks LeanAgent parity with Pi `packages/ai`. It is the source of
truth for preventing AI-module omissions.

Reference root: `vendor/pi/packages/ai`

Current Pi package version: `@earendil-works/pi-ai` `0.80.2`

## Status Legend

| Status | Meaning |
| --- | --- |
| `implemented` | Lean has equivalent behavior and tests. |
| `partial` | Lean has a subset, or behavior is API-compatible only for selected providers. |
| `missing` | No Lean equivalent exists yet. |
| `deferred` | Intentionally not implemented in the current milestone, with a reason. |

Every row must move through this ledger before it is considered complete.

## Current Lean Coverage

| Pi area | Lean modules | Status | Notes |
| --- | --- | --- | --- |
| HTTP transport | `LeanAgent.Http`, `LeanAgent.AI.Util.Proxy` | partial | Native libcurl JSON POST plus generic raw HTTP requests now exist, including arbitrary methods, raw bodies, raw Authorization headers, custom request headers, parsed response headers, no-proxy support, proxy env helper logic, and AWS event-stream response normalization into JSON for Bedrock-style buffered runtimes. Streaming callbacks, transport abort/cancel parity, and explicit proxy injection for non-libcurl transports are still missing. |
| OpenAI-compatible chat completions | `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.OpenAI`, `LeanAgent.Models` | partial | Protocol logic now lives under `AI.Api`; legacy `LeanAgent.OpenAI` is a compatibility wrapper. Non-streaming Chat Completions, streaming request path through `streamSimple`, SSE chunk parsing, text/thinking/tool delta event reconstruction, tool calls, empty-tools omission, tool history handling, basic option/header serialization, prompt cache payload fields, session-affinity headers for compatible models, usage parsing including `prompt_tokens_details.cache_write_tokens` from both chunk-level and choice-level streaming usage fallbacks without double-counting `reasoning_tokens`, model-rate cost calculation through Models runtime, retry policy, provider error diagnostics, header-only auth dispatch, typed legacy alias dispatch, direct context-aware request conversion for OpenAI-compatible dispatch, reasoning-model instruction-role selection between `developer` and `system`, user image content serialization, grouped tool-result image replay, thinking-as-text assistant replay including thinking-only serialization and runtime dispatch, encrypted `reasoning_details` to tool-call `thoughtSignature` parsing plus replay, OpenCode Go streaming `reasoning` delta normalization to `reasoning_content` plus assistant replay normalization while generic providers preserve `reasoning`, Xiaomi MiMo empty `reasoning_content` assistant replay coverage, OpenRouter-style routed `chunk.model` capture on `responseModel` while preserving the requested model id, null SSE chunk tolerance plus Pi-style provider `finish_reason` error mapping and missing-finish termination errors, stable-index and id-keyed coalescing for mixed parallel `tool_calls` deltas, Groq Qwen3 clamp-to-default `reasoning_effort` handling, Moonshot K2.6 disabled-thinking preservation plus K2.7 Code off-null omission coverage through built-in runtime dispatch, Anthropic-style cache markers on compatible models, strict-mode tool-definition suppression, Cloudflare AI Gateway `/compat` conservative payload fields plus BYOK `Authorization` preservation and session-affinity runtime headers, built-in z.ai GLM-5.2 `reasoning_effort` mappings plus selective `tool_stream` gating for unsupported models such as `glm-4.5-air`, explicit z.ai `tool_stream` compat override handling, model thinking-map-aware suppression for compat payloads with explicit `mapped := none`, non-reasoning chat-template omission, explicit default-value compat override parity for `reasoning_effort` and `max_tokens` across runtime plus legacy alias dispatch, compat-gated streaming `stream_options` emission, tool-result `name` replay plus required assistant bridge insertion after tool results, OpenRouter `provider` routing passthrough, Vercel gateway `providerOptions.gateway` routing passthrough, and OpenAI Responses request/stream dispatch exist. Native callback-style live streaming transport and the full compat matrix are still missing. |
| Static model catalog | `LeanAgent.Models` | partial | Starter catalog covers DeepSeek, the current Pi OpenAI Responses model set, the current Pi OpenAI Codex model set, the current Pi GitHub Copilot mixed OpenAI/Responses/Anthropic model set, Azure OpenAI Responses current Pi model set, OpenRouter, Groq, xAI, Cerebras, Together, the current Pi Fireworks mixed Anthropic/OpenAI catalog slice including Anthropic-compatible Kimi/router models plus OpenAI-compatible `glm-5p2`, current Pi OpenAI-compatible catalogs for Ant Ling, Hugging Face, Moonshot AI/CN, NVIDIA including `NVCF-POLL-SECONDS`, Xiaomi, Xiaomi Token Plan AMS/CN/SGP, Z.AI, and Z.AI Coding CN, selected Anthropic Claude models, current Pi Anthropic-compatible catalogs for Kimi Coding, MiniMax, MiniMax CN, and Vercel AI Gateway, the mixed-API OpenCode/OpenCode Go catalogs with explicit checked-in OpenAI-compatible compat metadata like `maxTokensField`, explicit default-value override markers for Together/Z.AI/OpenCode compat edges, built-in OpenRouter Kimi K2.6/K2.7 Code plus Xiaomi MiMo reasoning-replay compat metadata coverage, the current Pi Google Generative AI/Gemini model set, the current Pi Google Vertex Gemini model set, and the current Pi Mistral model set. Generated full catalog and dynamic refresh are missing. |
| Provider/model collection | `LeanAgent.Models`, `LeanAgent.AI.Providers.*` | partial | Runtime provider collection now supports registration, lookup, refresh hooks, auth application, auth-driven base URL/header overrides, auth-aware model listing with registered OAuth provider model hooks plus OAuth-derived allowed-model filtering/base-URL rewriting for built-in providers like GitHub Copilot, catalog-level and model-level provider headers, registry-based built-in OAuth auth adaptation without direct `LeanAgent.Models` imports of the Anthropic/GitHub Copilot/OpenAI Codex implementation modules, Pi-style separation of provider stream adapters into `LeanAgent.AI.Providers.Streams`, Pi-style separation of built-in auth/catalog-provider assembly into `LeanAgent.AI.Providers.Catalog`, OpenAI-compatible compat metadata including session-affinity headers for Cloudflare Workers AI / AI Gateway, Anthropic-compatible session-affinity compat wiring, simple stream/complete dispatch, explicit provider factories for the expanded OpenAI-compatible provider family, GitHub Copilot mixed OpenAI/Responses/Anthropic provider wiring with env-key and stored OAuth auth, Anthropic catalog provider wiring with env-key and stored OAuth auth, Anthropic-compatible provider factories for Kimi Coding/MiniMax/MiniMax CN/Vercel AI Gateway, mixed-API OpenCode/OpenCode Go catalog provider factories, OpenAI Responses provider/catalog dispatch, OpenAI Codex Responses provider/catalog dispatch with SSE fallback transport, Anthropic Messages buffered SSE dispatch, Google Generative AI buffered SSE dispatch, Google Vertex buffered SSE dispatch including ADC token acquisition from service-account and authorized-user local credentials, Mistral Conversations buffered SSE dispatch, Azure OpenAI Responses provider/catalog dispatch, generic OpenAI Responses dispatch, Azure OpenAI Responses stream dispatch, and Cloudflare Workers AI / AI Gateway provider factories for currently implemented OpenAI-compatible/Responses protocols. Local `Models` collection dispatch coverage now exercises built-in Anthropic, Google, Google Vertex, Mistral, and Bedrock provider factories through auth application into their real provider runtimes, in addition to existing OpenAI-family coverage. `LeanAgent.AI.Providers.All.builtinProviders` now matches Pi `providers/all.ts`, including `opencode` and `opencode-go` in the 35-provider built-in family, and it now exposes a TS-style `getBuiltinModel` lookup alongside `getBuiltinModel?`. OpenAI-compatible, Responses, Codex SSE fallback, Anthropic, Google, Vertex, Mistral, and Azure `streamSimple` paths use streaming request formats but still buffer the HTTP response before returning events. Full generated catalog tooling, dynamic providers, legacy CLI dispatch for non-Chat-Completions providers, OpenAI Codex WebSocket/cached transport, and callback-style live streaming are missing. |
| Agent-facing messages | `LeanAgent.Core`, `LeanAgent.AI.Types` | partial | Pi-style message/content/usage/diagnostic types and legacy conversions exist. Runtime still uses simplified `Core.AgentMessage`. |
| Images | `LeanAgent.AI.Types`, `LeanAgent.AI.Images`, `LeanAgent.AI.Images.Registry`, `LeanAgent.AI.Api.OpenRouterImages`, `LeanAgent.AI.Images.Models` | partial | Image content/model/option/result types, global image API registry, API mismatch guard, source unregister/reset with built-in replay, runtime image provider collection, auth application, `generateImages` dispatch, OpenRouter Images provider factory/runtime, and the current Pi OpenRouter image model catalog exist. Shared auth can resolve and refresh stored OAuth credentials with image collection test coverage; image collection/direct dispatch now honors abort signals and aborted results, but provider-specific OAuth image behavior and live provider matrix tests remain missing. |
| OAuth/auth store | `LeanAgent.AI.Auth`, `LeanAgent.AI.OAuth`, `LeanAgent.AI.OAuth.Anthropic`, `LeanAgent.AI.OAuth.GitHubCopilot`, `LeanAgent.AI.OAuth.OpenAICodex`, `LeanAgent.AI.OAuth.LocalCallback`, `LeanAgent.AI.OAuth.PKCE`, `LeanAgent.AI.OAuth.Page`, `LeanAgent.AI.EnvApiKeys`, `LeanAgent.AI.Providers.CloudflareAuth` | partial | Env API-key auth, Pi provider env-key map, auth context with injectable wall-clock milliseconds, in-memory and file-backed API-key/OAuth credentials, atomic temp-file persistence with rename, cross-process store locking, provider auth resolution, OAuth refresh/toAuth resolution under credential-store modify, lazy OAuth loading, OAuth provider registry, OAuth provider info list, high-level refresh/API-key helpers, device-code polling, callback success/error page rendering, PKCE verifier/challenge generation, GitHub Copilot built-in device-code login/refresh with verification-URI validation and available-model discovery, Anthropic built-in token refresh plus browser/manual-code login with localhost callback/manual-input race and abortable manual-code prompt handling, OpenAI Codex token refresh plus device-code and browser/manual-code login with localhost callback/manual-input race and abortable manual-code prompt handling, access-token validation via JWT account-id extraction, stored OAuth `toAuth`, OAuth-derived allowed-model/base-URL metadata for auth-aware model listing, model-aware auth resolution, Cloudflare Workers AI / AI Gateway auth helpers, and typed `ModelsError(auth|oauth)` wrapping for credential-store read/modify, API-key resolve, OAuth refresh, and OAuth `toAuth` failures now exist. Broader OAuth login providers and per-provider lock granularity are still missing. |
| Compat/global API registry | `LeanAgent.AI.Compat`, `LeanAgent.AI.Compat.Aliases` | partial | Global API provider registry, built-in Anthropic/OpenAI-compatible/OpenAI Responses/OpenAI Codex Responses/Azure OpenAI Responses/Google/Google Vertex/Mistral/Bedrock registrations, source-id unregister, reset, generic `stream`/`complete` plus `streamSimple`/`completeSimple` dispatch, fixed-API legacy aliases, Anthropic/Bedrock Converse Stream/Azure OpenAI Responses/OpenAI Completions/OpenAI Responses/OpenAI Codex Responses/Google/Google Vertex/Mistral typed legacy `stream*`/`complete*` aliases plus fixed-API `streamSimple*`/`completeSimple*` aliases, re-export of the core AI barrel surface, image registry/generation/catalog passthroughs, API mismatch guard, and Pi mapped provider env API-key injection exist. Broader non-stream legacy export parity, legacy static catalog exports, and several non-OpenAI builtins are still missing. |

## Implementation Gates

Before implementing Pi `packages/agent` features that depend on AI:

1. Define AI types first: content blocks, messages, tools, usage, stream events, stop reasons.
2. Add a stream/result abstraction, even if an API starts as non-streaming internally.
3. Move auth resolution out of `Main.lean` and into a Pi-style provider/model layer.
4. Implement OpenAI-compatible APIs as API modules, not as CLI-specific provider helpers.
5. Add tests mapped from Pi tests for every completed row.

Before starting OMP advanced work:

1. `Models` collection must support provider registration, lookup, auth application, and completion dispatch.
2. OpenAI-compatible provider family must be solid enough for DeepSeek, OpenAI, OpenRouter, Groq, xAI, Cerebras, Together, and similar providers.
3. Agent core must consume AI stream events rather than direct provider responses.

## Source Inventory

| Group | Pi files | Lean target | Current status |
| --- | ---: | --- | --- |
| Root entrypoints and package API | 16 | `LeanAgent.lean`, `LeanAgent.Models`, `LeanAgent.AI.Compat`, `LeanAgent.AI.Providers.All`, docs | partial |
| API protocol implementations | 28 | `LeanAgent.AI.Api.*` | partial |
| Auth | 5 | `LeanAgent.AI.Auth.*` | partial |
| Providers and model catalogs | 74 | `LeanAgent.AI.Providers.*`, generated/catalog data | partial |
| Utils | 14 | `LeanAgent.AI.Util.*` | partial |
| Tests | 282 | `Tests.lean`, future focused test modules | partial |

## Root Entrypoints

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `src/index.ts` | `LeanAgent.AI`, `LeanAgent.lean` import/export barrels | partial | Lean now has a dedicated `LeanAgent/AI.lean` core barrel alongside the wider `LeanAgent.lean` root barrel. The AI barrel directly brings in the main API option modules plus auth/types/event-stream/session/faux/json-parse/diagnostics/retry and OAuth type surface used by Pi `src/index.ts`, and it no longer directly imports compat or the built-in OAuth implementation barrel. It now imports the full `LeanAgent.Models` module so both runtime collection helpers and the checked-in static catalog surface are available from `import LeanAgent.AI`, but root-barrel parity is still incomplete because `LeanAgent.lean` remains a wider app-facing surface. |
| `src/compat.ts` | `LeanAgent.AI.Compat` | partial | Global registry, generic `stream`/`complete` plus `streamSimple`/`completeSimple` dispatch, direct `LeanAgent.AI.Compat` barrel import of the core compat surface plus the side-effect-free `LeanAgent.AI` core barrel, root-level lazy API wrapper re-exports for the core text providers, TS-style `getApiProvider`/`getImagesApiProvider` lookups alongside Lean `...?` helpers plus static catalog `getModel`/`getImageModel` option lookups, direct `provider.stream` / `provider.streamSimple` methods with API mismatch guards, env API-key helpers, root-level legacy alias re-exports, static catalog passthroughs, image registry/generation/catalog passthroughs including explicit built-in image re-registration, faux provider registration/unregistration, built-in Anthropic plus Bedrock/OpenAI-family registration, Pi-style builtin-model routing through the builtin `Models` collection when the active API provider is still the builtin implementation, typed legacy alias `complete*`/`completeSimple*` helpers alongside the existing stream aliases, and the Bedrock lazy override hook now exist. Broader non-stream legacy export parity is still missing. |
| `src/cli.ts` | future `lean-agent ai ...` commands | missing | Not needed for core loop yet. |
| `src/models.ts` | `LeanAgent.Models`, `LeanAgent.Models.Core` | partial | Lean now splits Pi-style runtime core from builtin catalog/default assembly: `LeanAgent.Models.Core` carries `ModelInfo`/`Provider`/`Collection`, `createModels`, `createProvider`, auth application, auth-aware model listing with registered OAuth provider hooks plus OAuth-derived base-URL and allowed-model filtering, shared cost/thinking helpers, and simple completion dispatch. Request paths now turn setup failures like unknown providers and auth-resolution errors into error streams instead of throwing. `LeanAgent.Models` keeps static provider info, checked-in catalogs, built-in auth assembly, and legacy selection. Current OpenAI Responses, OpenAI Codex, GitHub Copilot, Azure, Mistral, Google, Vertex, OpenCode/OpenCode Go, image, and Bedrock checked-in catalog slices exist; generated refresh parity and dynamic provider behavior are still missing. |
| `src/models.generated.ts` | generated Lean catalog or checked-in catalog | partial | Lean has checked-in catalog slices for implemented providers, including current Pi GitHub Copilot and Bedrock models. A generator and full generated catalog parity are still missing. |
| `src/types.ts` | `LeanAgent.AI.Types` | partial | Core content/message/usage/stream-option types and image model/option/result types exist. Provider-specific compat and full stream runtime still missing. |
| `src/env-api-keys.ts` | `LeanAgent.AI.EnvApiKeys`, `LeanAgent.AI.Auth` | partial | Full Pi provider env-key map exists, including Anthropic OAuth-token precedence, `findEnvKeys`, `getEnvApiKey`, Bedrock ambient credential marker, and Vertex explicit/default ADC path marker. File-backed API-key/OAuth storage exists in `LeanAgent.AI.Auth`; OAuth login flows are still tracked separately. |
| `src/session-resources.ts` | `LeanAgent.AI.SessionResources` | implemented | Global cleanup registry exists with unregister handles, optional session id propagation, and aggregate cleanup errors. |
| `src/legacy-api-aliases.ts` | `LeanAgent.AI.Compat`, `LeanAgent.AI.Compat.Aliases` | partial | Fixed-API legacy aliases exist for Anthropic, Bedrock Converse Stream, Azure OpenAI Responses, Google, Google Vertex, Mistral, OpenAI Codex Responses, OpenAI Completions, and OpenAI Responses. `LeanAgent.AI.Compat` now exposes direct root-level forwards for the alias functions while `LeanAgent.AI.Compat.Aliases` remains the underlying namespace; typed aliases accept provider-specific options for both `stream*` and `complete*`, and fixed-API `streamSimple*`/`completeSimple*` aliases remain available for simple option dispatch. Full static legacy export parity beyond this alias surface is still incomplete. |
| `src/oauth.ts` | `LeanAgent.AI.OAuth` | partial | OAuth credential/auth resolution contracts exist in `LeanAgent.AI.Auth`; Lean now also has a standalone OAuth type surface in `LeanAgent.AI.OAuth.Types`, plus provider interface, provider registry, register/unregister/reset/list/info helpers, `refreshOAuthToken`, `getOAuthApiKey` with expiry refresh, device-code polling, registered OAuth provider model hooks, and a Pi-style `LeanAgent.AI.OAuth` entrypoint that brings in the built-in Anthropic/GitHub Copilot/OpenAI Codex providers plus the shared localhost-callback, PKCE, and OAuth page helpers on direct import. Lean also has a real GitHub Copilot built-in provider with device-code login and refresh plus login-signal-aware polling, OAuth-derived allowed-model/base-URL metadata that now feeds model listing, a real Anthropic built-in provider with refresh plus browser/manual-code login, localhost callback/manual-code race handling, and abortable manual-code prompts, and a real OpenAI Codex built-in provider with refresh plus device-code and browser/manual-code login, localhost callback/manual-code race handling, and abortable manual-code prompts. Broader provider refresh implementations are still missing. |
| `src/bedrock-provider.ts` | `LeanAgent.AI.Providers.AmazonBedrock` | partial | Amazon Bedrock provider factory, current Pi model catalog, AWS ambient/stored bearer auth semantics, SigV4 request preparation helpers, shared credentials/config profile resolution, built-in/compat registration, and buffered ConverseStream runtime over native AWS event-stream decoding now exist. Full AWS SDK/default credential chain parity, proxy-aware Smithy middleware, and live abort/callback transport semantics are still missing. |
| `src/image-models.ts` | `LeanAgent.AI.Images.Models` | implemented | Static image provider/model lookup helpers exist for the current Pi image catalog. |
| `src/image-models.generated.ts` | `LeanAgent.AI.Images.Models` | implemented | Current Pi generated OpenRouter image model list is checked in as Lean data with 37 models. |
| `src/images-models.ts` | `LeanAgent.AI.Images`, `LeanAgent.AI.Images.Models` | partial | Runtime image model type, lookup functions, mutable image provider collection, provider CRUD, model refresh, auth application, and error-as-result `generateImages` exist. Shared OAuth credential resolution/refresh is covered for image collections; image-specific OAuth providers remain missing. |
| `src/images.ts` | `LeanAgent.AI.Images` | partial | `generateImages` and explicit `generateImagesWithApi` dispatch through registered image API providers. OpenRouter Images is registered as a built-in provider. |
| `src/images-api-registry.ts` | `LeanAgent.AI.Images.Registry` | implemented | Image API provider registry supports register, lookup, list, source unregister, clear/reset, built-in replay, and Pi-style API mismatch wrapping. |

## Types Layer

Pi `src/types.ts` is a hard prerequisite. Lean should add `LeanAgent/AI/Types.lean`
before expanding provider behavior.

| Contract | Lean target | Status | Notes |
| --- | --- | --- | --- |
| API/provider identifiers | `LeanAgent.AI.Types` | implemented | Central string aliases exist for text and image APIs/providers. |
| Text/image content blocks | `LeanAgent.AI.Types` | implemented | Text, thinking, image, and tool-call content blocks exist with JSON helpers. |
| User/assistant/tool result messages | `LeanAgent.AI.Types` | partial | Pi-style messages exist with JSON helpers. Runtime migration from `Core.AgentMessage` is not complete. |
| Tool schema and tool call content | `LeanAgent.AI.Types`, `LeanAgent.AI.Schema`, `LeanAgent.AI.Validation` | partial | Tool call exists, with JSON Schema subset validation/coercion including a string `pattern` matcher subset. Full TypeBox/AJV parity is missing. |
| Assistant stream events | `LeanAgent.AI.Types`, `LeanAgent.AI.EventStream`, `LeanAgent.AI.Util.SSE`, `LeanAgent.Loop` | partial | Event data types, stream/result container, partial snapshots, SSE parser, OpenAI streaming response parser, and agent-loop bridge exist. Async iteration and native callback-style live provider streaming are still missing. |
| Usage and cost | `LeanAgent.AI.Types`, `LeanAgent.Core`, `LeanAgent.Models`, `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.AI.Api.OpenAIResponses`, `LeanAgent.AI.Providers.Streams` | partial | Usage/cost types, JSON helpers, legacy provider usage bridge, OpenAI-compatible token parsing, Responses usage parsing, OpenAI-compatible model-rate cost calculation through Models runtime, Responses service-tier cost multipliers, and local provider-stream cost coverage for Anthropic Messages, Google Generative AI, Google Vertex, Mistral Conversations, and Bedrock ConverseStream now exist. Cross-provider live-abort cost semantics and broader provider-matrix coverage are still incomplete. |
| Stop reasons and errors | `LeanAgent.AI.Types`, `LeanAgent.AI.Util.Diagnostics`, `LeanAgent.Http` | partial | Stop reason types, assistant diagnostics, provider error extraction, transport response header capture, cross-provider `onResponse` hook observation, and Collection/Compat-level response status/header diagnostics for provider failures now exist, including OpenAI Codex HTTP error paths. Exact JS-style error stacks and broader direct-API assistant diagnostic surfacing are still incomplete. |
| Thinking/reasoning levels | `LeanAgent.AI.Types`, `LeanAgent.Models` | partial | Thinking level types, model `thinkingLevelMap` entries, supported-level filtering, xhigh opt-in, null suppression, simple-option reasoning clamp, and OpenAI-compatible/Responses payload alias mapping exist. Full generated metadata and non-OpenAI protocol wiring are still incomplete. |
| Simple stream options | `LeanAgent.AI.Types` | partial | Core option fields exist, including abort signal fields. Collection/compat preflight aborts, retry-sleep aborts, provider-specific option passthrough, and direct buffered API preflight abort coverage for Anthropic Messages, Google Generative AI, Google Vertex, Mistral Conversations, and Bedrock ConverseStream now exist. Native live transport abort behavior and broader provider-specific option parity are still missing. |

## Models Runtime

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `Provider` interface | `LeanAgent.Models.Provider` | partial | Runtime unit has id/name/base metadata, auth, model listing, refresh hook, and generic `stream`/`complete` convenience methods alongside `streamSimple`/`completeSimple`. Richer typed stream APIs and live async streams are still missing. |
| `Models` interface | `LeanAgent.Models.Collection` | partial | Collection supports provider registration, TS-style `getProvider`/`getModel` lookups alongside Lean `...?` helpers, best-effort model listing, refresh, auth application, generic `stream`/`complete`, and `streamSimple`/`completeSimple`. Single-provider refresh now wraps failures as `ModelsError(model_source)` while refresh-all remains best-effort and concurrently waits all registered refresh hooks. |
| `MutableModels` | `LeanAgent.Models.Collection` | implemented | `setProvider`, `deleteProvider`, and `clearProviders` exist with tests through runtime dispatch. |
| `createModels` | `LeanAgent.Models.Collection` | implemented | Constructor accepts credential store/auth context and defaults to in-memory credentials. |
| `createProvider` | `LeanAgent.Models.Provider` | partial | Single/mapped API dispatch, including the mixed OpenCode/OpenCode Go catalog routes, refresh state, lazy setup/load error streams, and Pi-style in-flight dynamic refresh dedupe now exist. Dynamic generated-provider parity is still missing. |
| `hasApi` | `LeanAgent.Models` | implemented | Runtime API equality helper exists. |
| `calculateCost` | `LeanAgent.Models` | implemented | Pi-style per-million token cost calculation exists, including 1h cache-write multiplier. |
| `getSupportedThinkingLevels` | `LeanAgent.Models` | implemented | Pi-style non-reasoning/off behavior, default xhigh suppression, explicit xhigh opt-in, and null map suppression exist. |
| `clampThinkingLevel` | `LeanAgent.Models` | implemented | Pi-style clamp over supported model thinking levels exists, including upward fallback before downward fallback. |
| `modelsAreEqual` | `LeanAgent.Models` | implemented | Compares provider and model id. |

## Auth Layer

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `auth/types.ts` | `LeanAgent.AI.Auth`, `LeanAgent.AI.Auth.OAuthBridge` | implemented | API-key credential, OAuth credential with extra-field preservation, auth result, provider auth, auth context, prompt/event/login callback contracts, callback-driven OAuth auth login, and the auth-to-OAuth bridge all exist. `envApiKeyAuth` now exposes secret-prompt login, and provider OAuth login can surface auth URLs, device codes, progress, select prompts, and manual-code prompts through the auth layer. |
| `auth/context.ts` | `LeanAgent.AI.Auth` | implemented | Default env/file-existence context matches Pi's trimmed env lookup, leading-`~` home expansion, and false-on-missing file checks; browser-specific fallback is not relevant to Lean. |
| `auth/credential-store.ts` | `LeanAgent.AI.Auth` | partial | In-memory and JSON file-backed API-key/OAuth credential stores exist, including OAuth extra-field persistence, atomic temp-file writes with rename, in-process serialized `modify`/`delete` access that prevents double-refresh races, and cross-process lock-directory coordination for file-backed stores. Per-provider lock granularity is still missing. |
| `auth/helpers.ts` | `LeanAgent.AI.Auth`, `LeanAgent.AI.Providers.CloudflareAuth` | partial | Env API-key auth helper, secret-prompt API-key login, callback-forwarding lazy OAuth wrapper with load-once caching, model-aware base URL auth resolution, and Cloudflare Workers AI / AI Gateway auth helpers exist. Most ambient provider helpers are missing. |
| `auth/resolve.ts` | `LeanAgent.AI.Auth` | partial | API-key provider auth resolution now accepts Pi-style model context through `ModelRef` while preserving the legacy wrapper with optional model base URL context. Stored OAuth credentials now own the provider, refresh under credential-store `modify` when expired, persist refreshed credentials, derive request auth through `toAuth`, and emit typed `ModelsError(auth|oauth)` failures for credential-store read/modify, API-key resolve, OAuth refresh, and OAuth auth-derivation paths. Broader typed auth surface is still incomplete. |

## API Protocol Implementations

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `api/lazy.ts` | `LeanAgent.AI.Api.Lazy`, `LeanAgent.Models.ProviderStreams.lazy` | implemented | Lean does not dynamically import TS modules, but the equivalent boundary now exists at the same surface level: `LeanAgent.AI.Api.Lazy.lazyApi` wraps `ProviderStreams.lazy`, setup/load failures become assistant error streams, and the checked-in lazy API wrappers route through that helper instead of throwing. |
| `api/simple-options.ts` | `LeanAgent.AI.Api.SimpleOptions`, `LeanAgent.AI.Types`, `LeanAgent.Models`, `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.AI.Api.OpenAIResponses` | partial | Simple option fields exist and are applied to OpenAI-compatible and Responses payloads/headers. Context-aware `maxTokens` clamping now uses estimated context tokens plus a 4096-token safety margin, preserves explicit request caps, defaults to model max output when known, and avoids emitting a synthetic cap when model max output is unknown. `onPayload` and `onResponse` hooks can inspect/replace JSON payloads and observe HTTP status/headers on both OpenAI-compatible and Responses runtimes. `adjustMaxTokensForThinking` matches Pi default/custom thinking budgets and xhigh-to-high clamping. Abort signals now propagate through simple-option conversions and retry-sleep handling, but native provider transport abort behavior is still missing. |
| `api/openai-completions.ts` | `LeanAgent.AI.Api.OpenAICompletions` | partial | Refactored into an API module with legacy wrapper, payload/header serialization, non-streaming completion, streaming request/response parsing, text/thinking/tool delta events, tool calls, empty-tools behavior, tool-history tools array, prompt cache payload fields, compatible-model session-affinity headers, usage parsing, retry policy, provider error diagnostics, reasoning/max-token/temperature/tool-choice options, surrogate-sanitized legacy user/assistant/tool payload strings, thinking-level payload aliases/off mapping through Models dispatch, typed legacy alias dispatch, direct context-aware message conversion, user image payload parts, grouped tool-result image replay for OpenAI-compatible requests, thinking-as-text replay, encrypted `reasoning_details` binding/replay, Anthropic-style cache markers, compat-driven strict omission, compat-driven `tool_stream`, thinking-map-aware suppression when compat explicitly disables a level with `mapped := none`, non-reasoning chat-template omission, and response parsing. Native callback-style live SSE transport and full compat matrix are still missing. |
| `api/openai-completions.lazy.ts` | `LeanAgent.AI.Api.OpenAICompletionsLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in OpenAI-compatible implementation. JS-style dynamic import is not needed. |
| `api/openai-responses.ts` | `LeanAgent.AI.Api.OpenAIResponses`, `LeanAgent.Models`, `LeanAgent.AI.Providers.OpenAI` | partial | Responses request payload construction, prompt-cache/session affinity fields, cache-affinity/custom headers, GitHub Copilot dynamic headers, non-streaming HTTP completion, provider error handling, usage parsing, service-tier cost multipliers, thinking-level payload aliases/off-null semantics, reasoning/message/function-call output parsing, streaming SSE event parsing, terminal-event enforcement, tool-call argument delta handling, HTTP streaming wrapper, generic Models dispatch, typed legacy alias dispatch, OpenAI provider factory, `OPENAI_API_KEY` auth, current Pi OpenAI Responses model catalog, and local provider dispatch tests exist. Native callback-style live streaming, generated catalog refresh tooling, legacy CLI Responses selection, and full provider matrix coverage are missing. |
| `api/openai-responses-shared.ts` | `LeanAgent.AI.Api.OpenAIResponsesShared` | partial | Shared message/tool serialization now converts system/user/assistant/tool-result replay items, consumes `TransformMessages`, generates unique fallback `msg_pi_*` message ids for replayed assistant text blocks, normalizes OpenAI Responses tool-call IDs including foreign `fc_<hash>` item IDs, omits different-model `fc_` item IDs to avoid pairing validation, serializes image-capable tool outputs, and converts tools with `strict`. Full Responses request runtime remains in `LeanAgent.AI.Api.OpenAIResponses`; broader shared streaming conversion parity is still incomplete. |
| `api/openai-responses.lazy.ts` | `LeanAgent.AI.Api.OpenAIResponsesLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Responses implementation. JS-style dynamic import is not needed. |
| `api/openai-codex-responses.ts` | `LeanAgent.AI.Api.OpenAICodexResponses`, `LeanAgent.Models`, `LeanAgent.AI.Providers.OpenAICodex` | partial | Codex base URL normalization, JWT account-id extraction, Codex request payload fields (`instructions`, `text.verbosity`, encrypted reasoning include, prompt cache key, auto tool choice, parallel tool calls), thinking-level mapping, ChatGPT account/originator/beta/session headers, SSE fallback transport through shared Responses stream parser, `response.done` terminal-event handling, non-`2xx` HTTP provider error handling, service-tier cost reuse, current Pi Codex model catalog, OAuth stored access-token `toAuth`, provider factory, compat built-in dispatch, typed legacy alias dispatch, response hooks, collection-level response-header diagnostics, and local SSE/error tests exist. WebSocket/cached transport, abort semantics, broader retry parity, and live provider tests are missing. |
| `api/openai-codex-responses.lazy.ts` | `LeanAgent.AI.Api.OpenAICodexResponsesLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Codex Responses implementation. JS-style dynamic import is not needed. |
| `api/azure-openai-responses.ts` | `LeanAgent.AI.Api.AzureOpenAIResponses`, `LeanAgent.Models`, `LeanAgent.AI.Providers.AzureOpenAIResponses` | partial | Azure base URL normalization, resource-name/default URL resolution, API-version query, deployment-name mapping, prompt-cache key clamp, `store=false`, `api-key` header transport, payload/response hooks via shared Responses runtime, streaming response parsing, compat built-in dispatch, typed legacy alias dispatch, provider factory, `AZURE_OPENAI_API_KEY` auth, and current Pi Azure model catalog integration exist. SDK abort semantics, generated catalog refresh tooling, and full dynamic provider behavior are missing. |
| `api/azure-openai-responses.lazy.ts` | `LeanAgent.AI.Api.AzureOpenAIResponsesLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Azure Responses implementation. JS-style dynamic import is not needed. |
| `api/anthropic-messages.ts` | `LeanAgent.AI.Api.AnthropicMessages` | partial | Anthropic Messages request payload/header construction, API-key/header-owned/OAuth-token auth header shapes, session-affinity header compat wiring, prompt cache `cache_control` payload markers for system prompts, last user blocks, last tool definitions with long-retention TTL support, surrogate-sanitized text/thinking/tool-result serialization, eager tool input streaming compat plus legacy fine-grained beta header fallback, interleaved-thinking beta header defaults with adaptive-thinking suppression, adaptive `output_config.effort` metadata, tool-level cache-control suppression, empty thinking-signature replay opt-in, cross-provider message transform reuse, system/tools/tool_result/tool_use conversion, non-streaming HTTP completion, buffered SSE request/response parsing, text/thinking/tool delta event reconstruction, usage mapping, stop-reason mapping, terminal `message_stop` validation, provider dispatch, typed legacy alias dispatch, and preflight abort coverage that skips payload hooks now exist. Native callback-style live streaming and remaining generated compat metadata are missing. |
| `api/anthropic-messages.lazy.ts` | `LeanAgent.AI.Api.AnthropicMessagesLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Anthropic implementation. JS-style dynamic import is not needed. |
| `api/google-generative-ai.ts` | `LeanAgent.AI.Api.GoogleGenerativeAI` | partial | Gemini REST `generateContent`/`streamGenerateContent?alt=sse` payload construction, `x-goog-api-key` headers, payload/response hooks, non-streaming response parsing, buffered SSE text/thinking/tool reconstruction, usage mapping, stop-reason mapping, provider dispatch, typed legacy alias dispatch, local HTTP tests, and direct API preflight abort coverage that skips payload hooks now exist. Native callback-style live streaming, transport-level abort during live requests, full SDK option surface, and Vertex/shared edge-case parity remain missing. |
| `api/google-generative-ai.lazy.ts` | `LeanAgent.AI.Api.GoogleGenerativeAILazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Google Generative AI implementation. JS-style dynamic import is not needed. |
| `api/google-vertex.ts` | `LeanAgent.AI.Api.GoogleVertex` | partial | Vertex direct REST `generateContent`/`streamGenerateContent?alt=sse` URL construction, project/location resolution, `{location}` base URL replacement, custom versioned collection base URLs, API-key headers, ADC placeholder marker handling, service-account JWT bearer exchange, authorized-user refresh-token exchange, payload construction via Google shared conversion, buffered SSE parsing via the Gemini parser, provider dispatch, typed legacy alias dispatch, local HTTP tests, and direct API preflight abort coverage that beats project/location resolution now exist. Google SDK resource-scope behavior, transport-level abort semantics during token fetch/live stream, and full Vertex option parity remain missing. |
| `api/google-vertex.lazy.ts` | `LeanAgent.AI.Api.GoogleVertexLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Google Vertex implementation. JS-style dynamic import is not needed. |
| `api/google-shared.ts` | `LeanAgent.AI.Api.GoogleShared` | partial | Gemini `Content[]`/`Part[]` conversion, user/model/functionResponse turns, image inline data, Gemini 2.x vs 3.x tool-result image routing, `isThinkingPart`/`retainThoughtSignature` helpers, unsigned Gemini 3 tool-call replay without synthetic validator text, matching-model thought-signature preservation, tool declarations, `parametersJsonSchema` default behavior, legacy OpenAPI `parameters` meta-declaration stripping, tool-choice mapping, cross-provider transform reuse, and stop-reason helpers exist. Vertex-specific tool compatibility and the remaining cross-model thought-signature replay matrix remain incomplete. |
| `api/mistral-conversations.ts` | `LeanAgent.AI.Api.MistralConversations` | partial | Mistral direct REST `/v1/chat/completions` payload/header construction, 9-character tool-call id normalization, system/user/assistant/tool conversion, tool schema serialization, provider-specific option mapping for `tool_choice`/`prompt_mode`/`reasoning_effort`, prompt cache `prompt_cache_key` and `x-affinity`, non-streaming response parsing, buffered SSE text/thinking/tool delta reconstruction, usage cache-read mapping, stop-reason mapping, provider dispatch, typed legacy alias dispatch, local HTTP tests, and direct API preflight abort coverage that skips payload hooks now exist. Native SDK/live-request abort semantics, callback-style live streaming, and SDK retry/request-option object semantics remain missing. |
| `api/mistral-conversations.lazy.ts` | `LeanAgent.AI.Api.MistralConversationsLazy` | partial | Lean now exposes a dedicated lazy wrapper module that returns `ProviderStreams.lazy` around the checked-in Mistral implementation. JS-style dynamic import is not needed. |
| `api/bedrock-converse-stream.ts` | `LeanAgent.AI.Api.BedrockConverseStream` | partial | ConverseStream command payload construction, text/image/tool/toolResult/thinking conversion, cache-point/reasoning/tool-choice/metadata helpers, stop-reason/endpoint/header helpers, GovCloud thinking-display omission, payload/response hooks, SigV4 request/auth preparation helpers, shared credentials/config profile resolution with explicit unresolved-profile failures, provider dispatch, typed legacy alias dispatch, buffered HTTP runtime, native AWS event-stream JSON normalization, streaming event parsing, local runtime tests, and direct API preflight abort coverage that skips payload hooks now exist. Real AWS SDK/default credential chain, Smithy proxy middleware, and live abort/callback transport semantics are still missing. |
| `api/bedrock-converse-stream.lazy.ts` | `LeanAgent.AI.Api.BedrockConverseStreamLazy` | partial | Lean now exposes a dedicated lazy wrapper module around the checked-in Bedrock implementation, including an override hook matching Pi's special bundling escape hatch at the API surface. |
| `api/cloudflare.ts` | `LeanAgent.AI.Api.Cloudflare` | implemented | Cloudflare Workers AI and AI Gateway base URL templates plus account/gateway env identifiers exist and are exercised by Cloudflare auth tests. |
| `api/github-copilot-headers.ts` | `LeanAgent.AI.Api.GitHubCopilotHeaders` | implemented | Copilot initiator inference, vision input detection, dynamic `X-Initiator`, `Openai-Intent`, and `Copilot-Vision-Request` headers exist, are wired into OpenAI Responses requests, and have local helper tests. |
| `api/openai-prompt-cache.ts` | `LeanAgent.AI.Api.OpenAIPromptCache` | partial | Prompt cache key clamping has direct helper tests, and Chat Completions payload fields exist, including `cacheRetention=none`, `long`, and `PI_CACHE_RETENTION=long`; Chat Completions and Anthropic-compatible session-affinity headers are wired through `sendSessionAffinityHeaders`; Anthropic Messages `cache_control` markers and long-retention TTL are wired for core payload positions, with `supportsCacheControlOnTools=false` suppressing tool-definition markers; provider-specific long-cache suppression is honored for OpenAI-compatible and Anthropic-compatible models. Broader generated compat metadata remains incomplete. |
| `api/openrouter-images.ts` | `LeanAgent.AI.Api.OpenRouterImages` | partial | OpenRouter Images Chat Completions payload construction, data-URL image inputs, modalities, API-key auth, custom headers, payload/response hooks, retry, provider diagnostics, response text/data-URL image parsing, usage parsing, and cost calculation exist. Abort signal preflight and aborted-result handling now exist; live provider matrix tests and native transport cancellation remain missing. |
| `api/openrouter-images.lazy.ts` | `LeanAgent.AI.Api.OpenRouterImagesLazy` | partial | Lean now exposes a dedicated image lazy wrapper module that returns `ProviderImages.lazy` around the checked-in OpenRouter Images implementation. JS-style dynamic import is not needed. |
| `api/transform-messages.ts` | `LeanAgent.AI.Api.TransformMessages` | partial | Provider-agnostic conversion covers non-vision image placeholders, cross-model thinking/text signature downgrade, foreign tool thought-signature removal, callback-driven tool-call id normalization with tool-result remapping, skipped errored/aborted assistant turns, and synthetic no-result tool results for orphaned calls. Provider-specific normalizers and live cross-provider handoff matrix are still missing. |

## Provider Factories

All provider factory files must have a corresponding row. Model catalog files
should be generated or checked in as Lean data.

| Provider | Pi files | Lean target | Status |
| --- | --- | --- | --- |
| All builtins | `providers/all.ts` | `LeanAgent.AI.Providers.All` | partial |
| Amazon Bedrock | `amazon-bedrock.ts`, `amazon-bedrock.models.ts` | `LeanAgent.AI.Providers.AmazonBedrock`, `LeanAgent.AI.Api.BedrockConverseStream`, `LeanAgent.Models` | partial |
| Ant Ling | `ant-ling.ts`, `ant-ling.models.ts` | `LeanAgent.AI.Providers.AntLing`, `LeanAgent.Models` | partial |
| Anthropic | `anthropic.ts`, `anthropic.models.ts` | `LeanAgent.AI.Providers.Anthropic`, `LeanAgent.AI.Api.AnthropicMessages`, `LeanAgent.AI.OAuth.Anthropic`, `LeanAgent.Models` | partial |
| Azure OpenAI Responses | `azure-openai-responses.ts`, `azure-openai-responses.models.ts` | `LeanAgent.AI.Api.AzureOpenAIResponses`, `LeanAgent.AI.Providers.AzureOpenAIResponses`, `LeanAgent.Models` | partial |
| Cerebras | `cerebras.ts`, `cerebras.models.ts` | `LeanAgent.AI.Providers.Cerebras`, `LeanAgent.Models` | partial |
| Cloudflare AI Gateway | `cloudflare-ai-gateway.ts`, `cloudflare-ai-gateway.models.ts` | `LeanAgent.AI.Providers.CloudflareAIGateway`, `LeanAgent.AI.Providers.CloudflareAuth` | partial |
| Cloudflare Workers AI | `cloudflare-workers-ai.ts`, `cloudflare-workers-ai.models.ts` | `LeanAgent.AI.Providers.CloudflareWorkersAI`, `LeanAgent.AI.Providers.CloudflareAuth` | partial |
| Cloudflare auth | `cloudflare-auth.ts` | `LeanAgent.AI.Providers.CloudflareAuth` | partial |
| DeepSeek | `deepseek.ts`, `deepseek.models.ts` | `LeanAgent.AI.Providers.DeepSeek`, `LeanAgent.Models` | partial |
| Faux test provider | `faux.ts` | `LeanAgent.AI.Providers.Faux` | partial |
| Fireworks | `fireworks.ts`, `fireworks.models.ts` | `LeanAgent.AI.Providers.Fireworks`, `LeanAgent.Models` | partial |
| GitHub Copilot | `github-copilot.ts`, `github-copilot.models.ts` | `LeanAgent.AI.Providers.GitHubCopilot`, `LeanAgent.AI.OAuth.GitHubCopilot` | partial |
| Google | `google.ts`, `google.models.ts` | `LeanAgent.AI.Providers.Google`, `LeanAgent.Models` | partial |
| Google Vertex | `google-vertex.ts`, `google-vertex.models.ts` | `LeanAgent.AI.Providers.GoogleVertex`, `LeanAgent.Models` | partial |
| Groq | `groq.ts`, `groq.models.ts` | `LeanAgent.AI.Providers.Groq`, `LeanAgent.Models` | partial |
| Hugging Face | `huggingface.ts`, `huggingface.models.ts` | `LeanAgent.AI.Providers.HuggingFace`, `LeanAgent.Models` | partial |
| Kimi Coding | `kimi-coding.ts`, `kimi-coding.models.ts` | `LeanAgent.AI.Providers.KimiCoding`, `LeanAgent.Models` | partial |
| MiniMax | `minimax.ts`, `minimax.models.ts` | `LeanAgent.AI.Providers.MiniMax`, `LeanAgent.Models` | partial |
| MiniMax CN | `minimax-cn.ts`, `minimax-cn.models.ts` | `LeanAgent.AI.Providers.MiniMaxCN`, `LeanAgent.Models` | partial |
| Mistral | `mistral.ts`, `mistral.models.ts` | `LeanAgent.AI.Providers.Mistral`, `LeanAgent.AI.Api.MistralConversations`, `LeanAgent.Models` | partial |
| Moonshot AI | `moonshotai.ts`, `moonshotai.models.ts` | `LeanAgent.AI.Providers.MoonshotAI`, `LeanAgent.Models` | partial |
| Moonshot AI CN | `moonshotai-cn.ts`, `moonshotai-cn.models.ts` | `LeanAgent.AI.Providers.MoonshotAICN`, `LeanAgent.Models` | partial |
| NVIDIA | `nvidia.ts`, `nvidia.models.ts` | `LeanAgent.AI.Providers.NVIDIA`, `LeanAgent.Models` | partial |
| OpenAI | `openai.ts`, `openai.models.ts` | `LeanAgent.AI.Providers.OpenAI`, `LeanAgent.Models` | partial |
| OpenAI Codex | `openai-codex.ts`, `openai-codex.models.ts` | `LeanAgent.AI.Providers.OpenAICodex`, `LeanAgent.AI.Api.OpenAICodexResponses`, `LeanAgent.Models` | partial |
| OpenCode | `opencode.ts`, `opencode.models.ts` | `LeanAgent.AI.Providers.OpenCode`, `LeanAgent.Models` | partial |
| OpenCode Go | `opencode-go.ts`, `opencode-go.models.ts` | `LeanAgent.AI.Providers.OpenCodeGo`, `LeanAgent.Models` | partial |
| OpenRouter | `openrouter.ts`, `openrouter.models.ts` | `LeanAgent.AI.Providers.OpenRouter`, `LeanAgent.Models` | partial |
| OpenRouter images | `openrouter-images.ts` | `LeanAgent.AI.Providers.OpenRouterImages` | partial |
| Together | `together.ts`, `together.models.ts` | `LeanAgent.AI.Providers.Together`, `LeanAgent.Models` | partial |
| Vercel AI Gateway | `vercel-ai-gateway.ts`, `vercel-ai-gateway.models.ts` | `LeanAgent.AI.Providers.VercelAIGateway`, `LeanAgent.Models` | partial |
| xAI | `xai.ts`, `xai.models.ts` | `LeanAgent.AI.Providers.XAI`, `LeanAgent.Models` | partial |
| Xiaomi | `xiaomi.ts`, `xiaomi.models.ts` | `LeanAgent.AI.Providers.Xiaomi`, `LeanAgent.Models` | partial |
| Xiaomi Token Plan AMS | `xiaomi-token-plan-ams.ts`, `xiaomi-token-plan-ams.models.ts` | `LeanAgent.AI.Providers.XiaomiTokenPlanAMS`, `LeanAgent.Models` | partial |
| Xiaomi Token Plan CN | `xiaomi-token-plan-cn.ts`, `xiaomi-token-plan-cn.models.ts` | `LeanAgent.AI.Providers.XiaomiTokenPlanCN`, `LeanAgent.Models` | partial |
| Xiaomi Token Plan SGP | `xiaomi-token-plan-sgp.ts`, `xiaomi-token-plan-sgp.models.ts` | `LeanAgent.AI.Providers.XiaomiTokenPlanSGP`, `LeanAgent.Models` | partial |
| ZAI | `zai.ts`, `zai.models.ts` | `LeanAgent.AI.Providers.ZAI`, `LeanAgent.Models` | partial |
| ZAI Coding CN | `zai-coding-cn.ts`, `zai-coding-cn.models.ts` | `LeanAgent.AI.Providers.ZAICodingCN`, `LeanAgent.Models` | partial |

## Utils

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `utils/abort-signals.ts` | `LeanAgent.AI.Util.Abort` | partial | Polling-based abort signal state, combined-signal composition, abort-aware sleeps, and shared abort-error detection now exist. Native transport cancellation and JS event-listener parity are still missing. |
| `utils/diagnostics.ts` | `LeanAgent.AI.Util.Diagnostics`, `LeanAgent.Http` | partial | Diagnostic error info, assistant message diagnostics, append helper, provider error body extraction, JSON thrown-value formatting/extraction, response header capture, and Collection/Compat-level error-to-diagnostic wiring now exist, including OpenAI Codex HTTP provider failures. Exact JS `Error` instance/stack behavior and broader direct-API assistant-level response-header diagnostics are incomplete. |
| `utils/estimate.ts` | `LeanAgent.AI.Util.Estimate` | implemented | Pi-style context/message/token estimation exists, including usage-anchor trailing-token logic, image estimate, UTF-16 text length behavior, and direct text-token estimate predictability coverage. |
| `utils/event-stream.ts` | `LeanAgent.AI.EventStream`, `LeanAgent.AI.Util.SSE`, `LeanAgent.Loop` | partial | Synchronous event/result container, partial snapshot reconstruction, buffered SSE response parsing, legacy provider wrapper, and loop consumption bridge exist. Async iteration/backpressure and live transport callbacks are not implemented. |
| `utils/hash.ts` | `LeanAgent.AI.Util.Hash` | implemented | Pi-compatible deterministic `shortHash`, including UTF-16 code-unit behavior, exists with golden tests. |
| `utils/headers.ts` | `LeanAgent.AI.Util.Headers`, `LeanAgent.Http`, `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.AI.Api.OpenAIResponses` | partial | Provider header filtering, case-insensitive merge/override behavior, custom request headers, response header parsing, and `onResponse` hook integration now have local runtime coverage across OpenAI-compatible, Responses, Azure OpenAI Responses, Anthropic, Google Generative AI, Google Vertex, Mistral, OpenAI Codex Responses, Bedrock, and OpenRouter Images. Remaining gaps are broader assistant-level diagnostic surfacing rather than transport/header plumbing itself. |
| `utils/json-parse.ts` | `LeanAgent.Json`, `LeanAgent.AI.Util.JsonParse` | partial | Malformed string repair, parse-with-repair, empty streaming fallback, partial object/array closure, dangling comma cleanup, dangling object-field omission, partial-literal truncation, and multi-pass repair/completion now exist. Full `partial-json` behavior for arbitrary incomplete structures is still incomplete. |
| `utils/node-http-proxy.ts` | `LeanAgent.AI.Util.Proxy` | partial | Provider-scoped/ambient proxy env lookup, default port mapping, `NO_PROXY` host/port matching, inferred proxy protocols, HTTP(S)-only validation, and URL-style resolved proxy normalization exist. Full WHATWG URL parsing and transport integration beyond libcurl are incomplete. |
| `utils/oauth/device-code.ts` | `LeanAgent.AI.OAuth` | partial | Device-code polling supports `pending`, `slow_down`, `failed`, `complete`, default/minimum polling intervals, slow-down interval growth, deadline-capped sleeps, timeout messages, injectable cancellation/clock/sleep for tests, and signal-driven mid-sleep cancellation. |
| `utils/oauth/github-copilot.ts` | `LeanAgent.AI.OAuth.GitHubCopilot` | partial | Domain normalization, OAuth endpoint URL construction, Copilot token `proxy-ep` base URL extraction, enterprise base URL fallback, selectable model response parsing, OAuth credential `toAuth`, credential-driven model filtering/base URL rewriting, verification-URI normalization/validation, device-code network login, GitHub access-token polling, Copilot token refresh, model-catalog-aware provider filtering, checked-in-catalog known-model enablement after login, and built-in OAuth provider registration with real login/refresh now exist. Broader provider parity and remaining non-Copilot OAuth providers are still incomplete. |
| `utils/oauth/anthropic.ts` | `LeanAgent.AI.OAuth.Anthropic` | partial | Token refresh, PKCE browser/manual-code login, localhost callback server, authorization-input parsing with state validation, callback/manual-input race handling, abortable manual-code prompt handling, stored OAuth access-token `toAuth`, and built-in OAuth provider registration now exist with local HTTP plus localhost callback coverage. Broader login-provider coverage is still missing. |
| `utils/oauth/openai-codex.ts` | `LeanAgent.AI.OAuth.OpenAICodex` | partial | Token refresh, device-code start/poll/exchange, PKCE browser/manual-code login, localhost callback server, authorization-input parsing with state validation, callback/manual-input race handling, abortable manual-code prompt handling, login-signal-aware device-code polling, access-token validation via JWT account-id extraction, and built-in OAuth provider registration now exist with local HTTP plus localhost callback coverage. Broader login-provider coverage is still missing. |
| `utils/oauth/oauth-page.ts` | `LeanAgent.AI.OAuth.Page` | implemented | Pi-style OAuth success/error callback HTML pages exist with escaping for all dynamic title, heading, message, and details fields, optional details rendering, and tests. |
| `utils/oauth/pkce.ts` | `LeanAgent.AI.OAuth.PKCE`, `native/http_client.c` | implemented | PKCE verifier generation uses OpenSSL `RAND_bytes`, S256 challenge generation uses OpenSSL `SHA256`, output is base64url without padding, and tests cover the RFC 7636 vector plus generated verifier/challenge shape. |
| `utils/overflow.ts` | `LeanAgent.AI.Util.Overflow` | partial | Common provider overflow messages, separator-insensitive Pi regex subset matching, non-overflow throttling/rate-limit exclusions, silent usage overflow, and length-stop full-context heuristics exist. Full JS regex semantics remains incomplete. |
| `utils/provider-env.ts` | `LeanAgent.AI.Util.ProviderEnv` | implemented | Scoped provider env lookup, ambient env fallback, empty-value suppression, and merge semantics exist. Bun-specific `/proc/self/environ` fallback is not applicable to Lean. |
| `utils/retry.ts` | `LeanAgent.AI.Util.Retry` | partial | Retryable assistant/provider error classification, separator-insensitive Pi regex subset matching, non-retryable quota guard, delay cap, and OpenAI-compatible request retry policy exist. Full JS regex semantics and cross-provider retry integration are incomplete. |
| `utils/sanitize-unicode.ts` | `LeanAgent.AI.Util.SanitizeUnicode` | partial | Lean string sanitizer preserves valid Unicode, a UTF-16 code-unit helper removes unpaired high/low surrogates while preserving valid pairs, and provider serialization is now wired through Anthropic Messages, OpenAI Completions legacy payloads, OpenAI Responses shared tool outputs, Google shared payloads, Mistral, Bedrock, and OpenRouter Images. Full provider coverage is still incomplete. |
| `utils/typebox-helpers.ts` | `LeanAgent.AI.Schema` | implemented | Pi's provider-compatible `StringEnum` helper is exposed as `stringEnum`, including optional description/default fields. |
| `utils/validation.ts` | `LeanAgent.AI.Validation`, `LeanAgent.AI.Schema` | partial | Tool lookup, argument validation, JSON Schema primitive coercion, required/object/array/enum/const checks, string/array/object bounds and `pattern` matching for anchored/unanchored literal, class, escape, and quantifier subsets, numeric inclusive/exclusive bounds, `uniqueItems`, `contains`/`minContains`/`maxContains`, `allOf` validation/coercion, `anyOf`/`oneOf` validation, `not`, and formatted validation errors exist. Full TypeBox compiler/AJV behavior, symbol metadata, branch coercion for `anyOf`/`oneOf`, full JS RegExp semantics, and all JSON Schema keywords are incomplete. |

## Images

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `images.ts` | `LeanAgent.AI.Images` | partial | Generic image generation dispatch exists, with OpenRouter Images registered as a built-in provider. |
| `images-models.ts` | `LeanAgent.AI.Images`, `LeanAgent.AI.Images.Models` | partial | Runtime image model type, TS-style `getProvider`/`getModel` lookups alongside Lean `...?` helpers, image provider collection, provider factory, refresh, auth merge, and abort-aware error-as-result dispatch exist. Image providers now share Pi-style in-flight refresh dedupe, single-provider `model_source` failure wrapping, and best-effort refresh-all behavior. Shared OAuth credential resolution/refresh has image collection tests; image-specific OAuth providers remain missing. |
| `image-models.ts` | `LeanAgent.AI.Images.Models` | implemented | Static image model access exists for the current Pi image catalog. |
| `image-models.generated.ts` | `LeanAgent.AI.Images.Models` | implemented | Current generated OpenRouter image catalog is represented as checked-in Lean data with 37 models. |
| `images-api-registry.ts` | `LeanAgent.AI.Images.Registry` | implemented | Register/get/list/unregister/clear/reset image API dispatch exists with built-in replay and API mismatch wrapping. |
| `api/openrouter-images.ts` | `LeanAgent.AI.Api.OpenRouterImages` | partial | OpenRouter Images request/runtime/parser behavior exists with local HTTP tests. Abort preflight and aborted-result handling now exist; live provider matrix tests are still missing. |
| `providers/openrouter-images.ts` | `LeanAgent.AI.Providers.OpenRouterImages` | partial | Built-in OpenRouter image API registration and Pi-style OpenRouter image provider factory with `OPENROUTER_API_KEY` auth and 37-model catalog exist, and the provider now routes through a dedicated `OpenRouterImagesLazy` wrapper instead of direct API dispatch. |

## Test Mapping

Initial Lean parity should port tests in this order:

| Pi tests | Lean target | Status | Why first |
| --- | --- | --- | --- |
| `models-runtime.test.ts`, `providers.test.ts`, `supports-xhigh.test.ts`, `xhigh.test.ts` | model catalog and thinking tests | partial | Protects provider/model registry, explicit OpenAI-compatible provider factories, OpenAI Responses catalog metadata, mixed OpenCode/OpenCode Go catalog/factory routing, dynamic refresh dedupe/failure semantics, cost calculation, thinking-level map helpers, auth-applied base URL overrides, and header-only auth dispatch. |
| `env-api-keys.test.ts`, `compat-env.test.ts`, Cloudflare auth tests | auth/env tests | partial | Env API-key, stored credential precedence, file-backed API-key/OAuth credential roundtrip/integration, OAuth refresh/toAuth resolution, lazy OAuth load-once delegation, OAuth credential ownership without env fallback, typed `ModelsError(auth|oauth)` wrapping for read/modify/resolve/refresh/`toAuth` failures, OAuth provider registry/high-level helper behavior, device-code polling behavior, callback page rendering/escaping, PKCE verifier/challenge behavior, GitHub Copilot OAuth helper parsing/model rewrite plus local device-code login/refresh, login-signal cancellation, verification-URI handling, and built-in-model collection filtering/base-URL rewriting from stored OAuth metadata, Anthropic OAuth refresh plus local browser/manual-code, localhost callback, abortable manual-code prompt, and state-validation coverage, OpenAI Codex OAuth refresh plus local browser/manual-code, localhost callback, abortable manual-code prompt, login-signal cancellation, and device-code login coverage, full provider env map including OpenCode/OpenCode Go shared `OPENCODE_API_KEY`, Anthropic OAuth-token precedence, Bedrock/Vertex ambient markers, Cloudflare Workers AI / AI Gateway auth resolution and factory wiring, compat registry dispatch, request API key pass-through, known-provider env key injection, catalog-outside provider env injection, and legacy alias registry dispatch are covered. Broader OAuth login providers remain missing. |
| `stream.test.ts`, `empty.test.ts`, `abort.test.ts` | event stream tests | partial | Stream result, text/thinking/tool events, partial snapshots, tool delta payloads, empty-content completion, and immediate abort results for collection/compat/Faux dispatch are covered in Lean. Async iterator/backpressure, native provider abort behavior, and live timing remain missing. |
| `openai-completions-*.test.ts` | OpenAI completions tests | partial | Payload tests cover empty tools, tool history, tool choice, max tokens, temperature, reasoning effort, thinking-level payload aliases, prompt cache key/retention/clamp helper behavior, session-affinity headers with caller override, direct context-aware user-image serialization, grouped tool-result image replay, thinking-as-text replay including thinking-only serialization and runtime dispatch, encrypted `reasoning_details` binding/replay, Anthropic-style cache markers, strict-mode suppression, Cloudflare AI Gateway `/compat` conservative OpenAI-compatible fields plus BYOK auth/header preservation and session-affinity runtime headers, built-in z.ai `tool_stream` gating plus explicit compat override and no-tools omission, chat-template omission for non-reasoning models, explicit `mapped := none` suppression for compat reasoning payloads, routed `responseModel` capture for OpenAI-compatible router ids, reasoning-model `developer` versus `system` instruction-role selection, Groq Qwen3 clamp-to-default `reasoning_effort`, built-in z.ai GLM-5.2 reasoning-effort mappings plus thinking-off omission, OpenCode Go assistant replay normalization from `reasoning` to `reasoning_content` while generic providers preserve `reasoning`, null chunk tolerance, Pi-style unknown `finish_reason` error mapping, missing `finish_reason` termination errors, stable-index and id-keyed mixed parallel `tool_calls` coalescing, chunk-level and choice-level `cache_write_tokens` streaming usage parsing plus no-double-count reasoning-token totals, streaming payload/SSE parsing, buffered streaming runtime dispatch, typed legacy alias dispatch, request/response headers, usage parsing, provider HTTP diagnostics, repaired tool arguments, and legacy assistant tool-call omission. Network provider matrix and true live-stream timing tests are missing. |
| `retry.test.ts`, `diagnostics.test.ts`, `estimate.test.ts`, `overflow.test.ts`, `validation.test.ts`, `unicode-surrogate.test.ts` | util tests | partial | Retry classifier/policy, abort helper combination, retry-delay abort cancellation, diagnostics extraction/round-trip, estimate utilities including text-token predictability, provider header filtering/merge, proxy env resolution, JSON repair/streaming fallback, JSON Schema validation/coercion including string pattern cases, Unicode surrogate sanitization helpers, and Anthropic/OpenAI Completions serialization coverage are covered alongside overflow detection and OpenAI transient HTTP retry. Live provider unicode-surrogate tests are missing. |
| `faux-provider.test.ts` | faux provider tests | partial | Deterministic provider handle, queued responses, helper blocks, model-aware factories, usage/cache estimates, model rewrite, collection dispatch, global compat registration/unregistration, event reconstruction, and abort preflight are covered. Async timing remains missing. |
| `session-resources.test.ts` | session resource cleanup tests | implemented | Cleanup registration, unregister handles, session id propagation, continued cleanup after failures, and aggregate errors are covered. |
| `images*.test.ts`, `openrouter-images.test.ts` | image tests | partial | Generic image API registry, built-in replay, image provider collection CRUD/refresh/auth merge/error result, shared OAuth credential resolution/refresh, collection abort preflight, OpenRouter provider factory auth, full current image catalog lookup/count, OpenRouter Images payload construction, direct abort-result handling, local HTTP dispatch, headers, response hooks, output parsing, usage/cost parsing, and missing-key errors are covered. Live provider matrix tests are still missing. |
| OpenAI/Anthropic/Google/Mistral/Bedrock/Azure/Codex tests | provider protocol tests | partial | OpenAI Responses local provider catalog/factory dispatch/typed legacy alias tests, OpenAI Responses shared foreign-toolcall/fallback-message-id tests, OpenAI Responses terminal-event parse/early-EOF compat error-stream tests, OpenAI Codex payload/header/helper/local SSE/provider catalog/stored OAuth/compat dispatch/typed legacy alias tests plus provider HTTP error/response-header diagnostics, local OAuth refresh, browser/manual-code login, localhost callback login, device-code login, and helpful 404 device-auth coverage, GitHub Copilot dynamic-header helper tests plus provider catalog/factory/env-key/stored-OAuth/builtin aggregation coverage, Anthropic Messages payload/header/session-affinity/prompt-cache/response-parser/SSE parser/local streaming/typed legacy alias/provider catalog tests plus local OAuth refresh, browser/manual-code login, localhost callback login, and state-mismatch coverage, Anthropic-compatible Kimi Coding/MiniMax/MiniMax CN/Vercel catalog/factory/env tests, mixed OpenCode/OpenCode Go catalog/factory/API-route/env tests, Google Generative AI payload/response-parser/SSE parser/local streaming/typed legacy alias/provider catalog tests, Google shared convertTools schema field/sanitization tests, Google shared thinking/image-routing/tool-call thought-signature tests, Google Vertex URL/header/auth/local streaming/typed legacy alias/provider catalog tests including service-account and authorized-user ADC token acquisition, Mistral Conversations payload/header/tool-choice/reasoning/prompt-cache/cache-retention/response-parser/SSE parser/local streaming/typed legacy alias/provider catalog tests, Bedrock payload/helper/catalog/auth/typed legacy alias plus buffered EventStream runtime, GovCloud thinking-display, SigV4 request-signing, and local runtime/compat coverage, Azure OpenAI Responses base URL, deployment mapping, payload, local streaming transport, typed legacy alias, provider catalog/factory, env auth, builtin aggregation, and compat built-in dispatch are covered. Codex WebSocket parity and full browser-automation parity are still missing. |

## Milestones

### M1: Core AI Types

Deliver:

- `LeanAgent.AI.Types` for content blocks, messages, tools, usage, stop reasons, stream options. Status: partial.
- Migration path from old `LeanAgent.Core.AgentMessage` to AI message types. Status: partial.
- Tests for serialization and basic content filtering. Status: partial.

Exit criteria:

- Existing CLI still runs.
- Old simplified messages are either wrappers around AI types or explicitly marked legacy.

### M2: Event Stream and Complete

Deliver:

- `LeanAgent.AI.EventStream` with event iteration/result semantics. Status: partial; sync stream containers and OpenAI SSE transcript reconstruction exist.
- Wrapper that turns current non-streaming `LeanAgent.OpenAI.provider` into a stream-compatible API. Status: partial; OpenAI streaming request/runtime path exists, native callback-style live transport is not wired.
- Tests mapped from `stream.test.ts` and `empty.test.ts`. Status: partial.

Exit criteria:

- Agent loop can consume stream events or complete results through the same boundary. Status: partial; current loop consumes buffered stream events, and OpenAI-compatible providers now use the streaming endpoint through `streamSimple`.

### M3: Models Collection and Auth

Deliver:

- Runtime `Models` collection with provider registration, lookup, refresh no-op for static providers. Status: partial; simple dispatch, lazy setup/load error streams, in-flight refresh dedupe, and single-provider `model_source` refresh failures now exist, but full dynamic provider behavior is still missing.
- Env API key auth, in-memory and file-backed API-key/OAuth credential stores, OAuth provider registry helpers, device-code polling, auth resolution. Status: partial; OAuth login provider flows are missing.
- `createProvider` for single API dispatch. Status: partial; API-map dispatch, mixed OpenCode/OpenCode Go catalog routing, lazy error stream behavior, and in-flight refresh dedupe exist, but dynamic provider parity is not complete.

Exit criteria:

- `Main.lean` no longer owns provider-specific auth/default resolution. Status: implemented for current DeepSeek/OpenAI CLI selection.
- Tests mapped from `models-runtime.test.ts`, `env-api-keys.test.ts`, `providers.test.ts`, `supports-xhigh.test.ts`, and `xhigh.test.ts`. Status: partial; auth resolution, stored/file credential precedence, runtime lookup/dispatch, cost calculation, and thinking-level map/clamp behavior are covered.

### M4: OpenAI-Compatible API Family

Deliver:

- Refactor `LeanAgent.OpenAI` into `LeanAgent.AI.Api.OpenAICompletions`. Status: implemented structurally; `LeanAgent.OpenAI` is now a compatibility wrapper.
- Add prompt cache fields, retry, response diagnostics, tool choice behavior, empty tools behavior. Status: partial; prompt cache payload fields, session-affinity headers for compatible OpenAI-compatible models, retry policy, basic provider diagnostics, response header capture, tool choice, typed legacy alias dispatch, and empty-tools behavior exist. Full response diagnostics are incomplete because provider callbacks/live stream diagnostics are missing.
- Add provider factories for DeepSeek, OpenAI, OpenRouter, Groq, xAI, Cerebras, Together, Fireworks, Ant Ling, Hugging Face, Moonshot AI/CN, NVIDIA, Xiaomi, Xiaomi Token Plan AMS/CN/SGP, Z.AI, Z.AI Coding CN, OpenCode, and OpenCode Go where they share OpenAI-compatible or mapped mixed protocols. Status: partial; provider modules and checked-in Pi catalog slices exist for these providers, model-level headers such as NVIDIA `NVCF-POLL-SECONDS` are applied through auth/dispatch, and Anthropic-backed Kimi Coding/MiniMax/MiniMax CN/Vercel AI Gateway plus Fireworks mixed Anthropic/OpenAI routing now exist under the Anthropic Messages or mapped OpenAI-compatible protocols. Generated refresh tooling remains missing.

Exit criteria:

- DeepSeek remains the default path. Status: implemented; catalog selection still checks DeepSeek first.
- OpenAI-compatible Pi tests that do not require live network are ported. Status: partial; payload, diagnostics, retry, Responses runtime dispatch, and provider-family catalog tests exist, but full non-network Pi test coverage is incomplete.

### M5: Provider Protocol Expansion

Deliver in this order:

- Anthropic Messages. Status: partial; payload/header/parser/provider dispatch, catalog-level provider headers, session-affinity compat wiring, prompt-cache cache-control payload markers, eager tool input streaming compat, interleaved-thinking beta headers, tool cache-control suppression, empty thinking-signature replay opt-in, Anthropic-compatible Kimi Coding/MiniMax/MiniMax CN/Vercel factories, mixed OpenCode/OpenCode Go Anthropic-routed catalog entries, adaptive-thinking compat metadata, temperature suppression for thinking/unsupported models, typed legacy alias dispatch, and buffered SSE event reconstruction exist. OAuth/live callback behavior and remaining generated compat metadata remain.
- Google Generative AI and Google Vertex. Status: partial; Google Generative AI core, Google/Vertex typed legacy alias dispatch, and Google Vertex direct REST/API-key/header-owned auth paths plus ADC token acquisition from service-account and authorized-user local credentials are implemented.
- Mistral Conversations. Status: partial; direct REST payload/header/parser/provider dispatch, Pi-defined option mapping, prompt-cache affinity behavior, typed legacy alias dispatch, and buffered SSE event reconstruction exist. SDK abort/live callback behavior and SDK request-option object semantics remain.
- Azure OpenAI Responses. Status: partial; Responses transport, Azure URL/deployment resolution, typed legacy alias dispatch, provider factory, auth, current Pi model catalog, and tests exist; SDK abort/dynamic catalog behavior remains.
- OpenAI Responses. Status: partial; provider factory, current Pi model catalog, auth, generic/local streaming dispatch, typed legacy alias dispatch, and tests exist; native callback streaming, generated catalog refresh, and legacy CLI Responses selection remain.
- OpenAI Codex Responses. Status: partial; provider factory, current Pi model catalog, stored OAuth access-token auth, Codex SSE fallback request/header semantics, shared Responses parsing, non-`2xx` HTTP provider error handling, compat dispatch, typed legacy alias dispatch, response hooks, collection-level response-header diagnostics, and local tests exist; WebSocket/cached transport plus broader abort/retry parity remain missing.
- Bedrock Converse Stream. Status: partial; provider factory, current Pi model catalog, AWS ambient/stored bearer auth, payload/helper conversion, GovCloud thinking-display omission, SigV4 request/auth preparation, shared credentials/config profile resolution, typed legacy alias dispatch, built-in/compat registration, native AWS event-stream decoding, buffered ConverseStream runtime, and local runtime coverage exist. AWS SDK default credential chain parity, Smithy proxy middleware, and live abort/callback transport semantics remain missing.

Exit criteria:

- Each provider has factory, model catalog, auth semantics, and protocol tests before marking implemented.

### M6: Images and Compat

Deliver:

- Image content support in message types.
- Image generation registry and OpenRouter image provider.
- Compat/global API registry after new `Models` API is stable. Status: partial; registry, reset, source unregister, built-in OpenAI-compatible/OpenAI Responses/Azure OpenAI Responses entries, simple dispatch, fixed-API legacy aliases, and env key injection exist.

Exit criteria:

- Image tests pass.
- Compat API is a thin wrapper over the new model collection, not a second runtime.

## Rules for Updating This Ledger

- Every AI change must update this file in the same commit when status changes.
- New Lean modules must cite the Pi source row they are closing in commit notes or PR text.
- A row can become `implemented` only with tests or an explicit reason why runtime validation is impossible.
- `vendor/pi` is read-only. Do not edit reference files.
- Prefer behavior parity over line-by-line TypeScript translation.
