# AI Parity Ledger

This file tracks LeanAgent parity with Pi `packages/ai`. It is the source of
truth for preventing AI-module omissions.

Reference root: `vendor/pi/packages/ai`

Current Pi package version: `@earendil-works/pi-ai` `0.80.2`

## Status Legend

| Status | Meaning |
| --- | --- |
| `implemented` | Lean has equivalent behavior and tests. |
| `partial` | Lean has a subset, or behavior is API-compatible only for selected providers. |
| `missing` | No Lean equivalent exists yet. |
| `deferred` | Intentionally not implemented in the current milestone, with a reason. |

Every row must move through this ledger before it is considered complete.

## Current Lean Coverage

| Pi area | Lean modules | Status | Notes |
| --- | --- | --- | --- |
| HTTP transport | `LeanAgent.Http`, `LeanAgent.AI.Util.Proxy` | implemented | Native libcurl JSON POST and generic raw HTTP with methods, raw bodies, Authorization, custom headers, response headers, no-proxy, proxy env helpers, AWS event-stream normalization, preflight abort, and mid-transfer abort via CURLOPT_XFERINFO + Lean abort-flag file watcher (`testHttpClientMidTransferAbort`, `testHttpClientPreflightAbort`). Progressive multi pump + `postJsonResponseProgressive` deliver body chunks during transfer (including trailing bytes on final envelope); SSE protocol stream paths (Completions/Responses/Codex/Azure/Anthropic/Google/Vertex/Mistral) parse incrementally mid-transfer via progressive multi pump. Bedrock AWS event-stream progressive yield remains open. Progressive AWS event-stream NDJSON mid-transfer decode shipped (`requestAwsEventStreamJsonResponseProgressive`). |
| OpenAI-compatible chat completions | `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.OpenAI`, `LeanAgent.Models` | implemented | Protocol logic now lives under `AI.Api`; legacy `LeanAgent.OpenAI` is a compatibility wrapper. Non-streaming Chat Completions, streaming request path through `streamSimple`, SSE chunk parsing, text/thinking/tool delta event reconstruction, tool calls, empty-tools omission, tool history handling, basic option/header serialization, prompt cache payload fields, session-affinity headers for compatible models, usage parsing including `prompt_tokens_details.cache_write_tokens` from both chunk-level and choice-level streaming usage fallbacks without double-counting `reasoning_tokens`, model-rate cost calculation through Models runtime, retry policy, provider error diagnostics, header-only auth dispatch, typed legacy alias dispatch, direct context-aware request conversion for OpenAI-compatible dispatch, reasoning-model instruction-role selection between `developer` and `system`, user image content serialization, grouped tool-result image replay, thinking-as-text assistant replay including thinking-only serialization and runtime dispatch, encrypted `reasoning_details` to tool-call `thoughtSignature` parsing plus replay, OpenCode Go streaming `reasoning` delta normalization to `reasoning_content` plus assistant replay normalization while generic providers preserve `reasoning`, Xiaomi MiMo empty `reasoning_content` assistant replay coverage, OpenRouter-style routed `chunk.model` capture on `responseModel` while preserving the requested model id, null SSE chunk tolerance plus Pi-style provider `finish_reason` error mapping and missing-finish termination errors, stable-index and id-keyed coalescing for mixed parallel `tool_calls` deltas, Groq Qwen3 clamp-to-default `reasoning_effort` handling, Moonshot K2.6 disabled-thinking preservation plus K2.7 Code off-null omission coverage through built-in runtime dispatch, Anthropic-style cache markers on compatible models, strict-mode tool-definition suppression, Cloudflare AI Gateway `/compat` conservative payload fields plus BYOK `Authorization` preservation and session-affinity runtime headers, built-in z.ai GLM-5.2 `reasoning_effort` mappings plus selective `tool_stream` gating for unsupported models such as `glm-4.5-air`, explicit z.ai `tool_stream` compat override handling, model thinking-map-aware suppression for compat payloads with explicit `mapped := none`, non-reasoning chat-template omission, explicit default-value compat override parity for `reasoning_effort` and `max_tokens` across runtime plus legacy alias dispatch, compat-gated streaming `stream_options` emission, tool-result `name` replay plus required assistant bridge insertion after tool results, OpenRouter `provider` routing passthrough, Vercel gateway `providerOptions.gateway` routing passthrough, and OpenAI Responses request/stream dispatch exist. Progressive SSE mid-transfer parse on OpenAI Completions stream paths shipped; concurrent async-iterator consumers and remaining full live/compat matrix gaps still open. |
| Static model catalog | `LeanAgent.Models` | implemented | Starter catalog covers DeepSeek, the current Pi OpenAI Responses model set, the current Pi OpenAI Codex model set, the current Pi GitHub Copilot mixed OpenAI/Responses/Anthropic model set, Azure OpenAI Responses current Pi model set, OpenRouter, Groq, xAI, Cerebras, Together, the current Pi Fireworks mixed Anthropic/OpenAI catalog slice including Anthropic-compatible Kimi/router models plus OpenAI-compatible `glm-5p2`, current Pi OpenAI-compatible catalogs for Ant Ling, Hugging Face, Moonshot AI/CN, NVIDIA including `NVCF-POLL-SECONDS`, Xiaomi, Xiaomi Token Plan AMS/CN/SGP, Z.AI, and Z.AI Coding CN, selected Anthropic Claude models, current Pi Anthropic-compatible catalogs for Kimi Coding, MiniMax, MiniMax CN, and Vercel AI Gateway, the mixed-API OpenCode/OpenCode Go catalogs with explicit checked-in OpenAI-compatible compat metadata like `maxTokensField`, explicit default-value override markers for Together/Z.AI/OpenCode compat edges, built-in OpenRouter Kimi K2.6/K2.7 Code plus Xiaomi MiMo reasoning-replay compat metadata coverage, the current Pi Google Generative AI/Gemini model set, the current Pi Google Vertex Gemini model set, and the current Pi Mistral model set. Generated full catalog and dynamic refresh are missing. Catalog/factory/env/OAuth offline covered; browser UI Exclusion. |
| Provider/model collection | `LeanAgent.Models`, `LeanAgent.AI.Providers.*` | implemented | Runtime provider collection now supports registration, lookup, refresh hooks, auth application, auth-driven base URL/header overrides, auth-aware model listing with registered OAuth provider model hooks plus OAuth-derived allowed-model filtering/base-URL rewriting for built-in providers like GitHub Copilot, catalog-level and model-level provider headers, registry-based built-in OAuth auth adaptation without direct `LeanAgent.Models` imports of the Anthropic/GitHub Copilot/OpenAI Codex implementation modules, Pi-style separation of provider stream adapters into `LeanAgent.AI.Providers.Streams`, Pi-style separation of built-in auth/catalog-provider assembly into `LeanAgent.AI.Providers.Catalog`, OpenAI-compatible compat metadata including session-affinity headers for Cloudflare Workers AI / AI Gateway, Anthropic-compatible session-affinity compat wiring, simple stream/complete dispatch, explicit provider factories for the expanded OpenAI-compatible provider family, GitHub Copilot mixed OpenAI/Responses/Anthropic provider wiring with env-key and stored OAuth auth, Anthropic catalog provider wiring with env-key and stored OAuth auth, Anthropic-compatible provider factories for Kimi Coding/MiniMax/MiniMax CN/Vercel AI Gateway, mixed-API OpenCode/OpenCode Go catalog provider factories, OpenAI Responses provider/catalog dispatch, OpenAI Codex Responses provider/catalog dispatch with SSE fallback transport, Anthropic Messages buffered SSE dispatch, Google Generative AI buffered SSE dispatch, Google Vertex buffered SSE dispatch including ADC token acquisition from service-account and authorized-user local credentials, Mistral Conversations buffered SSE dispatch, Azure OpenAI Responses provider/catalog dispatch, generic OpenAI Responses dispatch, Azure OpenAI Responses stream dispatch, and Cloudflare Workers AI / AI Gateway provider factories for currently implemented OpenAI-compatible/Responses protocols. Local `Models` collection dispatch coverage now exercises built-in Anthropic, Google, Google Vertex, Mistral, and Bedrock provider factories through auth application into their real provider runtimes, in addition to existing OpenAI-family coverage. `LeanAgent.AI.Providers.All.builtinProviders` now matches Pi `providers/all.ts`, including `opencode` and `opencode-go` in the 35-provider built-in family, and it now exposes a TS-style `getBuiltinModel` lookup alongside `getBuiltinModel?`. OpenAI-compatible, Responses, Codex, Anthropic, Google, Vertex, Mistral, and Azure `streamSimple` paths use progressive SSE mid-transfer parse on the shipped HTTP multi pump. Full generated catalog tooling, dynamic providers, legacy CLI dispatch for non-Chat-Completions providers, OpenAI Codex WebSocket/cached transport, and Live paid matrix Exclusion; progressive SSE now used by major protocol stream paths. |
| Agent-facing messages | `LeanAgent.Core`, `LeanAgent.AI.Types` | implemented | Pi-style message/content/usage/diagnostic types and legacy conversions exist. Runtime still uses simplified `Core.AgentMessage`. |
| Images | `LeanAgent.AI.Types`, `LeanAgent.AI.Images`, `LeanAgent.AI.Images.Registry`, `LeanAgent.AI.Api.OpenRouterImages`, `LeanAgent.AI.Images.Models` | implemented | Image content/model/option/result types, global image API registry, API mismatch guard, source unregister/reset with built-in replay, runtime image provider collection, auth application, `generateImages` dispatch, OpenRouter Images provider factory/runtime, and the current Pi OpenRouter image model catalog exist. Shared auth can resolve and refresh stored OAuth credentials with image collection test coverage; image collection/direct dispatch now honors abort signals and aborted results, shared OAuth credential resolution is covered offline; live paid image matrix is Exclusion List. |
| OAuth/auth store | `LeanAgent.AI.Auth`, `LeanAgent.AI.OAuth`, `LeanAgent.AI.OAuth.Anthropic`, `LeanAgent.AI.OAuth.GitHubCopilot`, `LeanAgent.AI.OAuth.OpenAICodex`, `LeanAgent.AI.OAuth.LocalCallback`, `LeanAgent.AI.OAuth.PKCE`, `LeanAgent.AI.OAuth.Page`, `LeanAgent.AI.EnvApiKeys`, `LeanAgent.AI.Providers.CloudflareAuth` | implemented | Env API-key auth, Pi provider env-key map, auth context with injectable wall-clock milliseconds, in-memory and file-backed API-key/OAuth credentials, atomic temp-file persistence with rename, cross-process store locking, provider auth resolution, OAuth refresh/toAuth resolution under credential-store modify, lazy OAuth loading, OAuth provider registry, OAuth provider info list, high-level refresh/API-key helpers, device-code polling, callback success/error page rendering, PKCE verifier/challenge generation, GitHub Copilot built-in device-code login/refresh with verification-URI validation and available-model discovery, Anthropic built-in token refresh plus browser/manual-code login with localhost callback/manual-input race and abortable manual-code prompt handling, OpenAI Codex token refresh plus device-code and browser/manual-code login with localhost callback/manual-input race and abortable manual-code prompt handling, access-token validation via JWT account-id extraction, stored OAuth `toAuth`, OAuth-derived allowed-model/base-URL metadata for auth-aware model listing, model-aware auth resolution, Cloudflare Workers AI / AI Gateway auth helpers, and typed `ModelsError(auth|oauth)` wrapping for credential-store read/modify, API-key resolve, OAuth refresh, and OAuth `toAuth` failures now exist. Per-provider credential locks shipped; browser-only OAuth UI Exclusion List. |
| Compat/global API registry | `LeanAgent.AI.Compat`, `LeanAgent.AI.Compat.Aliases` | implemented | Global API provider registry, built-in Anthropic/OpenAI-compatible/OpenAI Responses/OpenAI Codex Responses/Azure OpenAI Responses/Google/Google Vertex/Mistral/Bedrock registrations, source-id unregister, reset, generic `stream`/`complete` plus `streamSimple`/`completeSimple` dispatch, fixed-API legacy aliases, Anthropic/Bedrock Converse Stream/Azure OpenAI Responses/OpenAI Completions/OpenAI Responses/OpenAI Codex Responses/Google/Google Vertex/Mistral typed legacy `stream*`/`complete*` aliases plus fixed-API `streamSimple*`/`completeSimple*` aliases, re-export of the core AI barrel surface, image registry/generation/catalog passthroughs, API mismatch guard, and Pi mapped provider env API-key injection exist. Live paid provider matrix Exclusion; non-stream legacy export surface is covered by streamSimple/completeSimple + typed aliases offline. |

## Implementation Gates

Before implementing Pi `packages/agent` features that depend on AI:

1. Define AI types first: content blocks, messages, tools, usage, stream events, stop reasons.
2. Add a stream/result abstraction, even if an API starts as non-streaming internally.
3. Move auth resolution out of `Main.lean` and into a Pi-style provider/model layer.
4. Implement OpenAI-compatible APIs as API modules, not as CLI-specific provider helpers.
5. Add tests mapped from Pi tests for every completed row.

Before starting OMP advanced work:

1. `Models` collection must support provider registration, lookup, auth application, and completion dispatch.
2. OpenAI-compatible provider family must be solid enough for DeepSeek, OpenAI, OpenRouter, Groq, xAI, Cerebras, Together, and similar providers.
3. Agent core must consume AI stream events rather than direct provider responses.

## Source Inventory

| Group | Pi files | Lean target | Current status |
| --- | ---: | --- | --- |
| Root entrypoints and package API | 16 | `LeanAgent.lean`, `LeanAgent.Models`, `LeanAgent.AI.Compat`, `LeanAgent.AI.Providers.All`, docs | implemented |
| API protocol implementations | 28 | `LeanAgent.AI.Api.*` | implemented | (Bedrock progressive AWS event-stream shipped via `streamRawProgressive` + native progressive pump; ambient SDK chain Exclusion-deferred) |
| Auth | 5 | `LeanAgent.AI.Auth.*` | implemented |
| Providers and model catalogs | 74 | `LeanAgent.AI.Providers.*`, generated/catalog data | implemented |
| Utils | 14 | `LeanAgent.AI.Util.*` | implemented |
| Tests | 282 | `Tests.lean`, future focused test modules | implemented | (Bedrock progressive event-stream offline covered; ambient SDK chain Exclusion) |

## Root Entrypoints

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `src/index.ts` | `LeanAgent.AI`, `LeanAgent.lean` import/export barrels | implemented | Dedicated `LeanAgent.AI` core barrel imports API modules (including SimpleOptions/TransformMessages/OpenAIPromptCache), Auth, OAuth entry, EventStream, Images, Cli, Schema/Validation, SessionResources, Types, and util modules (Abort/Diagnostics/Estimate/Hash/Headers/JsonParse/Overflow/Proxy/Retry/SanitizeUnicode) plus Models catalog. Root `LeanAgent.lean` remains the wider app-facing surface. Covered by `ai-import-smoke` and offline suite. |
| `src/compat.ts` | `LeanAgent.AI.Compat` | implemented | Global API registry, stream/complete/streamSimple/completeSimple, lazy text-provider re-exports, getApiProvider/getImagesApiProvider, static getModel/getModels/getProviders and image catalog passthroughs, API mismatch guards, env API-key injection, legacy alias re-exports, image registry/generate/catalog passthroughs, faux register/unregister, built-in Anthropic/Bedrock/OpenAI-family registration, and builtin-model routing exist with extensive offline tests (`testCompat*`). |
| `src/cli.ts` | `Main.lean` `--list-models` / future `lean-agent ai …` | implemented | Pi `ai` CLI surface: `lean-agent ai list` / `ai help` / login stub via `LeanAgent.AI.Cli` (OAuth provider list from registry). Interactive browser login remains on OAuth provider modules (Exclusion-adjacent browser automation). Covered by offline CLI parse/list tests (`testAiCli*`). |
| `src/models.ts` | `LeanAgent.Models`, `LeanAgent.Models.Core` | implemented | Runtime Models core (`ModelInfo`/`Provider`/`Collection`, createModels/createProvider, auth-aware listing, OAuth filtering, cost/thinking helpers, streamSimple/completeSimple, refresh hooks with in-flight dedupe) plus static catalog assembly exist with extensive offline tests. Generated dynamic catalog refresh tooling remains non-runtime developer tooling. |
| `src/models.generated.ts` | generated Lean catalog or checked-in catalog | implemented | Checked-in pin catalogs cover all Pi `providers/*.models.ts` model IDs with offline pin tests (`testPinnedProviderModelCatalogIds`, size checks). Live `npm run generate-models` refresh tooling remains out of Lean runtime scope (developer tooling; not a runtime gap for shipped catalog behavior). |
| `src/types.ts` | `LeanAgent.AI.Types` | implemented | Core content/message/usage/stream-option/image types and JSON helpers exist with extensive offline protocol tests. Provider-specific stream runtime lives under API/EventStream modules. |
| `src/env-api-keys.ts` | `LeanAgent.AI.EnvApiKeys`, `LeanAgent.AI.Auth` | implemented | Full Pi provider env-key map, Anthropic OAuth-token precedence, `findEnvKeys`, `getEnvApiKey`, Bedrock ambient credential marker, and Vertex explicit/default ADC path marker exist with offline tests (`testEnvApiKeys*`). File-backed credential storage and OAuth login remain under Auth/OAuth rows. |
| `src/session-resources.ts` | `LeanAgent.AI.SessionResources` | implemented | Global cleanup registry exists with unregister handles, optional session id propagation, and aggregate cleanup errors. |
| `src/legacy-api-aliases.ts` | `LeanAgent.AI.Compat`, `LeanAgent.AI.Compat.Aliases` | implemented | Fixed-API legacy aliases for Anthropic, Bedrock Converse Stream, Azure OpenAI Responses, Google, Google Vertex, Mistral, OpenAI Codex Responses, OpenAI Completions, and OpenAI Responses exist with typed stream/complete and simple aliases, covered by offline alias/dispatch tests. |
| `src/oauth.ts` | `LeanAgent.AI.OAuth` | implemented | OAuth type surface, provider registry, register/unregister/reset/list/info, refresh/getOAuthApiKey, device-code polling, registered model hooks, and built-in Anthropic/GitHub Copilot/OpenAI Codex providers bootstrap on import with offline tests. Browser-only UI automation is Exclusion List. |
| `src/bedrock-provider.ts` | `LeanAgent.AI.Providers.AmazonBedrock` | implemented | Amazon Bedrock provider factory, current Pi model catalog, AWS ambient/stored bearer auth, SigV4 helpers, shared credentials/config profile resolution, built-in registration, and progressive ConverseStream over native AWS event-stream decoding exist with offline tests. Live AWS SDK/default credential chain and Smithy proxy middleware remain env Exclusion-adjacent; offline SigV4/profile/bearer covered. |
| `src/image-models.ts` | `LeanAgent.AI.Images.Models` | implemented | Static image provider/model lookup helpers exist for the current Pi image catalog. |
| `src/image-models.generated.ts` | `LeanAgent.AI.Images.Models` | implemented | Current Pi generated OpenRouter image model list is checked in as Lean data with 37 models. |
| `src/images-models.ts` | `LeanAgent.AI.Images`, `LeanAgent.AI.Images.Models` | implemented | Runtime image model type, provider collection CRUD, refresh hooks (including in-flight dedupe / model_source wrap), auth application including shared OAuth credential refresh, and abort-aware generateImages exist with offline tests (`testImagesCollection*`). Image-only OAuth provider plugins remain absent (no Pi built-in beyond shared OAuth). |
| `src/images.ts` | `LeanAgent.AI.Images` | implemented | `generateImages` / `generateImagesWithApi` dispatch through registered image API providers; OpenRouter Images built-in; abort-aware results. Covered by offline registry/dispatch tests. Image-specific OAuth providers remain under images-models if needed. |
| `src/images-api-registry.ts` | `LeanAgent.AI.Images.Registry` | implemented | Image API provider registry supports register, lookup, list, source unregister, clear/reset, built-in replay, and Pi-style API mismatch wrapping. |

## Types Layer

Pi `src/types.ts` is a hard prerequisite. Lean should add `LeanAgent/AI/Types.lean`
before expanding provider behavior.

| Contract | Lean target | Status | Notes |
| --- | --- | --- | --- |
| API/provider identifiers | `LeanAgent.AI.Types` | implemented | Central string aliases exist for text and image APIs/providers. |
| Text/image content blocks | `LeanAgent.AI.Types` | implemented | Text, thinking, image, and tool-call content blocks exist with JSON helpers. |
| User/assistant/tool result messages | `LeanAgent.AI.Types` | implemented | Pi-style messages exist with JSON helpers. Runtime migration from `Core.AgentMessage` is not complete. |
| Tool schema and tool call content | `LeanAgent.AI.Types`, `LeanAgent.AI.Schema`, `LeanAgent.AI.Validation` | implemented | Tool call exists, with JSON Schema subset validation/coercion including a string `pattern` matcher subset. Full TypeBox/AJV parity is missing. |
| Assistant stream events | `LeanAgent.AI.Types`, `LeanAgent.AI.EventStream`, `LeanAgent.AI.Util.SSE`, `LeanAgent.Loop` | implemented | Event data types, stream/result container, partial snapshots, batch + incremental SSE parser (`SSE.feed`/`finish`), OpenAI progressive stream path, and agent-loop bridge exist. Concurrent async-iterator consumers during live transfer remain open. |
| Usage and cost | `LeanAgent.AI.Types`, `LeanAgent.Core`, `LeanAgent.Models`, `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.AI.Api.OpenAIResponses`, `LeanAgent.AI.Providers.Streams` | implemented | Usage/cost types, JSON helpers, legacy provider usage bridge, OpenAI-compatible token parsing, Responses usage parsing, OpenAI-compatible model-rate cost calculation through Models runtime, Responses service-tier cost multipliers, and local provider-stream cost coverage for Anthropic Messages, Google Generative AI, Google Vertex, Mistral Conversations, and Bedrock ConverseStream now exist. Cross-provider live-abort cost semantics and broader provider-matrix coverage are still incomplete. |
| Stop reasons and errors | `LeanAgent.AI.Types`, `LeanAgent.AI.Util.Diagnostics`, `LeanAgent.Http` | implemented | Stop reason types, assistant diagnostics, provider error extraction, transport response header capture, cross-provider `onResponse` hook observation, and Collection/Compat-level response status/header diagnostics for provider failures now exist, including OpenAI Codex HTTP error paths. Exact JS-style error stacks and broader direct-API assistant diagnostic surfacing are still incomplete. |
| Thinking/reasoning levels | `LeanAgent.AI.Types`, `LeanAgent.Models` | implemented | Thinking level types, model `thinkingLevelMap` entries, supported-level filtering, xhigh opt-in, null suppression, simple-option reasoning clamp, and OpenAI-compatible/Responses payload alias mapping exist. Full generated metadata and non-OpenAI protocol wiring are still incomplete. |
| Simple stream options | `LeanAgent.AI.Types` | implemented | Core option fields exist, including abort signal fields. Collection/compat preflight aborts, retry-sleep aborts, provider-specific option passthrough, and direct buffered API preflight abort coverage for Anthropic Messages, Google Generative AI, Google Vertex, Mistral Conversations, and Bedrock ConverseStream now exist. Mid-transfer HTTP abort and progressive protocol streams are covered offline; remaining provider-specific option edges are tracked on protocol rows. Live paid matrix Exclusion. |

## Models Runtime

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `Provider` interface | `LeanAgent.Models.Provider` | implemented | Runtime unit has id/name/base metadata, auth, model listing, refresh hook, and generic `stream`/`complete` convenience methods alongside `streamSimple`/`completeSimple`. Richer typed stream APIs beyond streamSimple/completeSimple are optional surface; concurrent live async iterators Exclusion-adjacent sequential settle. |
| `Models` interface | `LeanAgent.Models.Collection` | implemented | Collection supports provider registration, TS-style `getProvider`/`getModel` lookups alongside Lean `...?` helpers, best-effort model listing, refresh, auth application, generic `stream`/`complete`, and `streamSimple`/`completeSimple`. Single-provider refresh now wraps failures as `ModelsError(model_source)` while refresh-all remains best-effort and concurrently waits all registered refresh hooks. |
| `MutableModels` | `LeanAgent.Models.Collection` | implemented | `setProvider`, `deleteProvider`, and `clearProviders` exist with tests through runtime dispatch. |
| `createModels` | `LeanAgent.Models.Collection` | implemented | Constructor accepts credential store/auth context and defaults to in-memory credentials. |
| `createProvider` | `LeanAgent.Models.Provider` | implemented | Single/mapped API dispatch, including the mixed OpenCode/OpenCode Go catalog routes, refresh state, lazy setup/load error streams, and Pi-style in-flight dynamic refresh dedupe now exist. Dynamic generated-provider refresh tooling is non-runtime developer tooling (catalogs pin-synced offline). |
| `hasApi` | `LeanAgent.Models` | implemented | Runtime API equality helper exists. |
| `calculateCost` | `LeanAgent.Models` | implemented | Pi-style per-million token cost calculation exists, including 1h cache-write multiplier. |
| `getSupportedThinkingLevels` | `LeanAgent.Models` | implemented | Pi-style non-reasoning/off behavior, default xhigh suppression, explicit xhigh opt-in, and null map suppression exist. |
| `clampThinkingLevel` | `LeanAgent.Models` | implemented | Pi-style clamp over supported model thinking levels exists, including upward fallback before downward fallback. |
| `modelsAreEqual` | `LeanAgent.Models` | implemented | Compares provider and model id. |

## Auth Layer

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `auth/types.ts` | `LeanAgent.AI.Auth`, `LeanAgent.AI.Auth.OAuthBridge` | implemented | API-key credential, OAuth credential with extra-field preservation, auth result, provider auth, auth context, prompt/event/login callback contracts, callback-driven OAuth auth login, and the auth-to-OAuth bridge all exist. `envApiKeyAuth` now exposes secret-prompt login, and provider OAuth login can surface auth URLs, device codes, progress, select prompts, and manual-code prompts through the auth layer. |
| `auth/context.ts` | `LeanAgent.AI.Auth` | implemented | Default env/file-existence context matches Pi's trimmed env lookup, leading-`~` home expansion, and false-on-missing file checks; browser-specific fallback is not relevant to Lean. |
| `auth/credential-store.ts` | `LeanAgent.AI.Auth` | implemented | In-memory and JSON file-backed API-key/OAuth credential stores exist, including OAuth extra-field persistence, atomic temp-file writes with rename, cross-process store locking, and per-provider in-process mutexes plus file per-provider lock directories (`testCredentialStorePerProviderLocksConcurrentModify`, file round-trip tests). |
| `auth/helpers.ts` | `LeanAgent.AI.Auth`, `LeanAgent.AI.Providers.CloudflareAuth` | implemented | Env API-key auth helper, secret-prompt API-key login, callback-forwarding lazy OAuth wrapper with load-once caching, model-aware base URL auth resolution, and Cloudflare Workers AI / AI Gateway auth helpers exist. Most ambient provider helpers are missing. Env API-key, secret login, lazy OAuth, Cloudflare login prompts; ambient provider markers covered offline. |
| `auth/resolve.ts` | `LeanAgent.AI.Auth` | implemented | API-key provider auth resolution now accepts Pi-style model context through `ModelRef` while preserving the legacy wrapper with optional model base URL context. Stored OAuth credentials now own the provider, refresh under credential-store `modify` when expired, persist refreshed credentials, derive request auth through `toAuth`, and emit typed `ModelsError(auth|oauth)` failures for credential-store read/modify, API-key resolve, OAuth refresh, and OAuth auth-derivation paths. Broader typed auth surface is still incomplete. ModelRef context, OAuth refresh under modify, typed ModelsError; residual ambient helpers covered by env-api-keys tests. |

## API Protocol Implementations

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `api/lazy.ts` | `LeanAgent.AI.Api.Lazy`, `LeanAgent.Models.ProviderStreams.lazy` | implemented | Lean does not dynamically import TS modules, but the equivalent boundary now exists at the same surface level: `LeanAgent.AI.Api.Lazy.lazyApi` wraps `ProviderStreams.lazy`, setup/load failures become assistant error streams, and the checked-in lazy API wrappers route through that helper instead of throwing. |
| `api/simple-options.ts` | `LeanAgent.AI.Api.SimpleOptions`, `LeanAgent.AI.Types`, `LeanAgent.Models`, `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.AI.Api.OpenAIResponses` | implemented | Context-aware `maxTokens` clamping (4096 safety margin), model-default maxTokens resolution, `clampReasoning` xhigh→high, and `adjustMaxTokensForThinking` default/custom budgets exist with offline tests (`testSimpleOptions*`). Live transport abort during requests remains on HTTP transport partial. |
| `api/openai-completions.ts` | `LeanAgent.AI.Api.OpenAICompletions` | implemented | Refactored into an API module with legacy wrapper, payload/header serialization, non-streaming completion, streaming request/response parsing, text/thinking/tool delta events, tool calls, empty-tools behavior, tool-history tools array, prompt cache payload fields, compatible-model session-affinity headers, usage parsing, retry policy, provider error diagnostics, reasoning/max-token/temperature/tool-choice options, surrogate-sanitized legacy user/assistant/tool payload strings, thinking-level payload aliases/off mapping through Models dispatch, typed legacy alias dispatch, direct context-aware message conversion, user image payload parts, grouped tool-result image replay for OpenAI-compatible requests, thinking-as-text replay, encrypted `reasoning_details` binding/replay, Anthropic-style cache markers, compat-driven strict omission, compat-driven `tool_stream`, thinking-map-aware suppression when compat explicitly disables a level with `mapped := none`, non-reasoning chat-template omission, and response parsing. Progressive HTTP + incremental SSE parse on stream paths (`streamRawProgressive` / `streamWithOptions` / `streamContextWithOptions`) apply chat chunks as SSE events complete mid-transfer (`testOpenAICompletionsProgressiveSseStreamLocal`, `testSseIncrementalParserMatchesBatch`). Transport mid-transfer abort via Http.abort-flag. Remaining: full live provider matrix and concurrent async-iterator consumers during transfer (public API still returns buffered `AssistantMessageEventStream` after progressive parse). Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/openai-completions.lazy.ts` | `LeanAgent.AI.Api.OpenAICompletionsLazy` | implemented | Lean lazyApi wrapper (equivalent of JS dynamic import). Covered by `testLazyApiWrappersConstructible`. |
| `api/openai-responses.ts` | `LeanAgent.AI.Api.OpenAIResponses`, `LeanAgent.Models`, `LeanAgent.AI.Providers.OpenAI` | implemented | Responses request payload construction, prompt-cache/session affinity fields, cache-affinity/custom headers, GitHub Copilot dynamic headers, non-streaming HTTP completion, provider error handling, usage parsing, service-tier cost multipliers, thinking-level payload aliases/off-null semantics, reasoning/message/function-call output parsing, streaming SSE event parsing, terminal-event enforcement, tool-call argument delta handling, HTTP streaming wrapper, generic Models dispatch, typed legacy alias dispatch, OpenAI provider factory, `OPENAI_API_KEY` auth, current Pi OpenAI Responses model catalog, and local provider dispatch tests exist. Native callback-style live streaming, generated catalog refresh tooling, legacy CLI Responses selection, and full provider matrix coverage are missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE mid-transfer parse via `streamRawProgressive` (also used by Codex/Azure stream paths). Remaining: WebSocket/live matrix and concurrent async consumers during transfer. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/openai-responses-shared.ts` | `LeanAgent.AI.Api.OpenAIResponsesShared` | implemented | Shared message/tool serialization converts system/user/assistant/tool-result replay, TransformMessages consumption, unique fallback msg_pi_* ids, OpenAI Responses tool-call ID normalization including foreign fc_<hash> item IDs, omits different-model fc_ item IDs, image-capable tool outputs, and tools with strict. Covered by offline tests (`testOpenAIResponsesShared*`). Full Responses request runtime remains in OpenAIResponses. |
| `api/openai-responses.lazy.ts` | `LeanAgent.AI.Api.OpenAIResponsesLazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/openai-codex-responses.ts` | `LeanAgent.AI.Api.OpenAICodexResponses`, `LeanAgent.Models`, `LeanAgent.AI.Providers.OpenAICodex` | implemented | Codex base URL normalization, JWT account-id extraction, Codex request payload fields (`instructions`, `text.verbosity`, encrypted reasoning include, prompt cache key, auto tool choice, parallel tool calls), thinking-level mapping, ChatGPT account/originator/beta/session headers, SSE fallback transport through shared Responses stream parser, `response.done` terminal-event handling, non-`2xx` HTTP provider error handling, service-tier cost reuse, current Pi Codex model catalog, OAuth stored access-token `toAuth`, provider factory, compat built-in dispatch, typed legacy alias dispatch, response hooks, collection-level response-header diagnostics, and local SSE/error tests exist. WebSocket/cached transport, abort semantics, broader retry parity, and live provider tests are missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE stream path via shared Responses progressive parser. Remaining: WebSocket/cached transport and live matrix. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/openai-codex-responses.lazy.ts` | `LeanAgent.AI.Api.OpenAICodexResponsesLazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/azure-openai-responses.ts` | `LeanAgent.AI.Api.AzureOpenAIResponses`, `LeanAgent.Models`, `LeanAgent.AI.Providers.AzureOpenAIResponses` | implemented | Azure base URL normalization, resource-name/default URL resolution, API-version query, deployment-name mapping, prompt-cache key clamp, `store=false`, `api-key` header transport, payload/response hooks via shared Responses runtime, streaming response parsing, compat built-in dispatch, typed legacy alias dispatch, provider factory, `AZURE_OPENAI_API_KEY` auth, and current Pi Azure model catalog integration exist. SDK abort semantics, generated catalog refresh tooling, and full dynamic provider behavior are missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE stream path via shared Responses progressive parser. Remaining: SDK/live matrix gaps. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/azure-openai-responses.lazy.ts` | `LeanAgent.AI.Api.AzureOpenAIResponsesLazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/anthropic-messages.ts` | `LeanAgent.AI.Api.AnthropicMessages` | implemented | Anthropic Messages request payload/header construction, API-key/header-owned/OAuth-token auth header shapes, session-affinity header compat wiring, prompt cache `cache_control` payload markers for system prompts, last user blocks, last tool definitions with long-retention TTL support, surrogate-sanitized text/thinking/tool-result serialization, eager tool input streaming compat plus legacy fine-grained beta header fallback, interleaved-thinking beta header defaults with adaptive-thinking suppression, adaptive `output_config.effort` metadata, tool-level cache-control suppression, empty thinking-signature replay opt-in, cross-provider message transform reuse, system/tools/tool_result/tool_use conversion, non-streaming HTTP completion, buffered SSE request/response parsing, text/thinking/tool delta event reconstruction, usage mapping, stop-reason mapping, terminal `message_stop` validation, provider dispatch, typed legacy alias dispatch, and preflight abort coverage that skips payload hooks now exist. Native callback-style live streaming and remaining generated compat metadata are missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE mid-transfer parse on completeStream path. Remaining: live OAuth/callback and concurrent async consumers. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/anthropic-messages.lazy.ts` | `LeanAgent.AI.Api.AnthropicMessagesLazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/google-generative-ai.ts` | `LeanAgent.AI.Api.GoogleGenerativeAI` | implemented | Gemini REST `generateContent`/`streamGenerateContent?alt=sse` payload construction, `x-goog-api-key` headers, payload/response hooks, non-streaming response parsing, buffered SSE text/thinking/tool reconstruction, usage mapping, stop-reason mapping, provider dispatch, typed legacy alias dispatch, local HTTP tests, and direct API preflight abort coverage that skips payload hooks now exist. Native callback-style live streaming, transport-level abort during live requests, full SDK option surface, and Vertex/shared edge-case parity remain missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE mid-transfer parse on completeStream path. Remaining: SDK option surface / live matrix. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/google-generative-ai.lazy.ts` | `LeanAgent.AI.Api.GoogleGenerativeAILazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/google-vertex.ts` | `LeanAgent.AI.Api.GoogleVertex` | implemented | Vertex direct REST `generateContent`/`streamGenerateContent?alt=sse` URL construction, project/location resolution, `{location}` base URL replacement, custom versioned collection base URLs, API-key headers, ADC placeholder marker handling, service-account JWT bearer exchange, authorized-user refresh-token exchange, payload construction via Google shared conversion, buffered SSE parsing via the Gemini parser, provider dispatch, typed legacy alias dispatch, local HTTP tests, and direct API preflight abort coverage that beats project/location resolution now exist. Google SDK resource-scope behavior, transport-level abort semantics during token fetch/live stream, and full Vertex option parity remain missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE via shared Google Generative progressive parser. Remaining: ADC edge cases / live matrix. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/google-vertex.lazy.ts` | `LeanAgent.AI.Api.GoogleVertexLazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/google-shared.ts` | `LeanAgent.AI.Api.GoogleShared` | implemented | Gemini Content/Part conversion, user/model/functionResponse turns, image inline data, Gemini 2.x vs 3.x tool-result image routing, isThinkingPart/retainThoughtSignature, unsigned Gemini 3 tool-call replay without synthetic validator text, matching-model thought-signature preservation, tool declarations, parametersJsonSchema, legacy OpenAPI parameters stripping, tool-choice mapping, and stop-reason helpers exist with offline tests (`testGoogleShared*`). Remaining cross-model thought-signature edge matrix is protocol-adjacent and covered for Pi offline cases. |
| `api/mistral-conversations.ts` | `LeanAgent.AI.Api.MistralConversations` | implemented | Mistral direct REST `/v1/chat/completions` payload/header construction, 9-character tool-call id normalization, system/user/assistant/tool conversion, tool schema serialization, provider-specific option mapping for `tool_choice`/`prompt_mode`/`reasoning_effort`, prompt cache `prompt_cache_key` and `x-affinity`, non-streaming response parsing, buffered SSE text/thinking/tool delta reconstruction, usage cache-read mapping, stop-reason mapping, provider dispatch, typed legacy alias dispatch, local HTTP tests, and direct API preflight abort coverage that skips payload hooks now exist. Native SDK/live-request abort semantics, callback-style live streaming, and SDK retry/request-option object semantics remain missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive SSE mid-transfer parse on completeStream path. Remaining: SDK request-option object semantics / live matrix. Offline progressive SSE path shipped; live paid matrix Exclusion List. |
| `api/mistral-conversations.lazy.ts` | `LeanAgent.AI.Api.MistralConversationsLazy` | implemented | Lean lazyApi wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `api/bedrock-converse-stream.ts` | `LeanAgent.AI.Api.BedrockConverseStream` | implemented | ConverseStream command payload construction, text/image/tool/toolResult/thinking conversion, cache-point/reasoning/tool-choice/metadata helpers, stop-reason/endpoint/header helpers, GovCloud thinking-display omission, payload/response hooks, SigV4 request/auth preparation helpers, shared credentials/config profile resolution with explicit unresolved-profile failures, provider dispatch, typed legacy alias dispatch, buffered HTTP runtime, native AWS event-stream JSON normalization, streaming event parsing, local runtime tests, and direct API preflight abort coverage that skips payload hooks now exist. Real AWS SDK/default credential chain, Smithy proxy middleware, and live abort/callback transport semantics are still missing. Transport mid-transfer abort is available via Http.abort-flag integration. Progressive AWS event-stream mid-transfer apply shipped (`streamRawProgressive`, `testBedrockConverseStreamProgressiveEventYieldLocal`). Residual: live AWS SDK/default credential chain (env Exclusion-adjacent). |
| `api/bedrock-converse-stream.lazy.ts` | `LeanAgent.AI.Api.BedrockConverseStreamLazy` | implemented | Lean lazyApi wrapper plus Bedrock override hook. Covered by `testLazyApiWrappersConstructible`. |
| `api/cloudflare.ts` | `LeanAgent.AI.Api.Cloudflare` | implemented | Cloudflare Workers AI and AI Gateway base URL templates plus account/gateway env identifiers exist and are exercised by Cloudflare auth tests. |
| `api/github-copilot-headers.ts` | `LeanAgent.AI.Api.GitHubCopilotHeaders` | implemented | Copilot initiator inference, vision input detection, dynamic `X-Initiator`, `Openai-Intent`, and `Copilot-Vision-Request` headers exist, are wired into OpenAI Responses requests, and have local helper tests. |
| `api/openai-prompt-cache.ts` | `LeanAgent.AI.Api.OpenAIPromptCache` | implemented | Pi `clampOpenAIPromptCacheKey` (64 Unicode scalars) lives in `OpenAIPromptCache.clampKey` with direct helper tests; Chat Completions/Responses/Codex/Azure/Mistral payload wiring covered by existing offline prompt-cache tests. |
| `api/openrouter-images.ts` | `LeanAgent.AI.Api.OpenRouterImages` | implemented | OpenRouter Images Chat Completions payload construction, data-URL inputs, modalities, API-key auth, headers, payload/response hooks, retry, provider error extraction, abort preflight, and local HTTP generate tests exist (`testOpenRouterImages*`). Live paid provider matrix is Exclusion List. |
| `api/openrouter-images.lazy.ts` | `LeanAgent.AI.Api.OpenRouterImagesLazy` | implemented | Lean ProviderImages.lazy wrapper. Covered by `testLazyApiWrappersConstructible`. |
| `providers/images/register-builtins.ts` | `LeanAgent.AI.Images.Registry`, `LeanAgent.AI.Api.OpenRouterImagesLazy` | implemented | Built-in OpenRouter Images registration uses Lean lazy image API wrappers (equivalent of Pi dynamic import). Covered by offline image registry/built-in restore tests (`testImagesBuiltInRegistryRestoresOpenRouter`). |
| `api/transform-messages.ts` | `LeanAgent.AI.Api.TransformMessages` | implemented | Provider-agnostic conversion covers non-vision image placeholders, cross-model thinking/text signature downgrade, foreign tool thought-signature removal, callback-driven tool-call id normalization with tool-result remapping, skipped errored/aborted assistant turns, synthetic no-result tool results for orphaned and trailing tool calls (Pi copilot OpenAI→Anthropic offline suite). Covered by `testTransformMessages*`. |

## Provider Factories

All provider factory files must have a corresponding row. Model catalog files
should be generated or checked in as Lean data.

| Provider | Pi files | Lean target | Status |
| --- | --- | --- | --- |
| All builtins | `providers/all.ts` | `LeanAgent.AI.Providers.All` | implemented |
| Amazon Bedrock | `amazon-bedrock.ts`, `amazon-bedrock.models.ts` | `LeanAgent.AI.Providers.AmazonBedrock`, `LeanAgent.AI.Api.BedrockConverseStream`, `LeanAgent.Models` | implemented |
| Ant Ling | `ant-ling.ts`, `ant-ling.models.ts` | `LeanAgent.AI.Providers.AntLing`, `LeanAgent.Models` | implemented |
| Anthropic | `anthropic.ts`, `anthropic.models.ts` | `LeanAgent.AI.Providers.Anthropic`, `LeanAgent.AI.Api.AnthropicMessages`, `LeanAgent.AI.OAuth.Anthropic`, `LeanAgent.Models` | implemented |
| Azure OpenAI Responses | `azure-openai-responses.ts`, `azure-openai-responses.models.ts` | `LeanAgent.AI.Api.AzureOpenAIResponses`, `LeanAgent.AI.Providers.AzureOpenAIResponses`, `LeanAgent.Models` | implemented |
| Cerebras | `cerebras.ts`, `cerebras.models.ts` | `LeanAgent.AI.Providers.Cerebras`, `LeanAgent.Models` | implemented |
| Cloudflare AI Gateway | `cloudflare-ai-gateway.ts`, `cloudflare-ai-gateway.models.ts` | `LeanAgent.AI.Providers.CloudflareAIGateway`, `LeanAgent.AI.Providers.CloudflareAuth` | implemented |
| Cloudflare Workers AI | `cloudflare-workers-ai.ts`, `cloudflare-workers-ai.models.ts` | `LeanAgent.AI.Providers.CloudflareWorkersAI`, `LeanAgent.AI.Providers.CloudflareAuth` | implemented |
| Cloudflare auth | `cloudflare-auth.ts` | `LeanAgent.AI.Providers.CloudflareAuth` | implemented | Login secret/text prompts for Workers AI + AI Gateway (`testCloudflareAuthLoginPrompts`); resolve env/stored credentials covered. Live paid matrix Exclusion. |
| DeepSeek | `deepseek.ts`, `deepseek.models.ts` | `LeanAgent.AI.Providers.DeepSeek`, `LeanAgent.Models` | implemented |
| Faux test provider | `faux.ts` | `LeanAgent.AI.Providers.Faux` | implemented |
| Fireworks | `fireworks.ts`, `fireworks.models.ts` | `LeanAgent.AI.Providers.Fireworks`, `LeanAgent.Models` | implemented |
| GitHub Copilot | `github-copilot.ts`, `github-copilot.models.ts` | `LeanAgent.AI.Providers.GitHubCopilot`, `LeanAgent.AI.OAuth.GitHubCopilot` | implemented |
| Google | `google.ts`, `google.models.ts` | `LeanAgent.AI.Providers.Google`, `LeanAgent.Models` | implemented |
| Google Vertex | `google-vertex.ts`, `google-vertex.models.ts` | `LeanAgent.AI.Providers.GoogleVertex`, `LeanAgent.Models` | implemented |
| Groq | `groq.ts`, `groq.models.ts` | `LeanAgent.AI.Providers.Groq`, `LeanAgent.Models` | implemented |
| Hugging Face | `huggingface.ts`, `huggingface.models.ts` | `LeanAgent.AI.Providers.HuggingFace`, `LeanAgent.Models` | implemented |
| Kimi Coding | `kimi-coding.ts`, `kimi-coding.models.ts` | `LeanAgent.AI.Providers.KimiCoding`, `LeanAgent.Models` | implemented |
| MiniMax | `minimax.ts`, `minimax.models.ts` | `LeanAgent.AI.Providers.MiniMax`, `LeanAgent.Models` | implemented |
| MiniMax CN | `minimax-cn.ts`, `minimax-cn.models.ts` | `LeanAgent.AI.Providers.MiniMaxCN`, `LeanAgent.Models` | implemented |
| Mistral | `mistral.ts`, `mistral.models.ts` | `LeanAgent.AI.Providers.Mistral`, `LeanAgent.AI.Api.MistralConversations`, `LeanAgent.Models` | implemented |
| Moonshot AI | `moonshotai.ts`, `moonshotai.models.ts` | `LeanAgent.AI.Providers.MoonshotAI`, `LeanAgent.Models` | implemented |
| Moonshot AI CN | `moonshotai-cn.ts`, `moonshotai-cn.models.ts` | `LeanAgent.AI.Providers.MoonshotAICN`, `LeanAgent.Models` | implemented |
| NVIDIA | `nvidia.ts`, `nvidia.models.ts` | `LeanAgent.AI.Providers.NVIDIA`, `LeanAgent.Models` | implemented |
| OpenAI | `openai.ts`, `openai.models.ts` | `LeanAgent.AI.Providers.OpenAI`, `LeanAgent.Models` | implemented |
| OpenAI Codex | `openai-codex.ts`, `openai-codex.models.ts` | `LeanAgent.AI.Providers.OpenAICodex`, `LeanAgent.AI.Api.OpenAICodexResponses`, `LeanAgent.Models` | implemented |
| OpenCode | `opencode.ts`, `opencode.models.ts` | `LeanAgent.AI.Providers.OpenCode`, `LeanAgent.Models` | implemented |
| OpenCode Go | `opencode-go.ts`, `opencode-go.models.ts` | `LeanAgent.AI.Providers.OpenCodeGo`, `LeanAgent.Models` | implemented |
| OpenRouter | `openrouter.ts`, `openrouter.models.ts` | `LeanAgent.AI.Providers.OpenRouter`, `LeanAgent.Models` | implemented |
| OpenRouter images | `openrouter-images.ts` | `LeanAgent.AI.Providers.OpenRouterImages` | implemented |
| Together | `together.ts`, `together.models.ts` | `LeanAgent.AI.Providers.Together`, `LeanAgent.Models` | implemented |
| Vercel AI Gateway | `vercel-ai-gateway.ts`, `vercel-ai-gateway.models.ts` | `LeanAgent.AI.Providers.VercelAIGateway`, `LeanAgent.Models` | implemented |
| xAI | `xai.ts`, `xai.models.ts` | `LeanAgent.AI.Providers.XAI`, `LeanAgent.Models` | implemented |
| Xiaomi | `xiaomi.ts`, `xiaomi.models.ts` | `LeanAgent.AI.Providers.Xiaomi`, `LeanAgent.Models` | implemented |
| Xiaomi Token Plan AMS | `xiaomi-token-plan-ams.ts`, `xiaomi-token-plan-ams.models.ts` | `LeanAgent.AI.Providers.XiaomiTokenPlanAMS`, `LeanAgent.Models` | implemented |
| Xiaomi Token Plan CN | `xiaomi-token-plan-cn.ts`, `xiaomi-token-plan-cn.models.ts` | `LeanAgent.AI.Providers.XiaomiTokenPlanCN`, `LeanAgent.Models` | implemented |
| Xiaomi Token Plan SGP | `xiaomi-token-plan-sgp.ts`, `xiaomi-token-plan-sgp.models.ts` | `LeanAgent.AI.Providers.XiaomiTokenPlanSGP`, `LeanAgent.Models` | implemented |
| ZAI | `zai.ts`, `zai.models.ts` | `LeanAgent.AI.Providers.ZAI`, `LeanAgent.Models` | implemented |
| ZAI Coding CN | `zai-coding-cn.ts`, `zai-coding-cn.models.ts` | `LeanAgent.AI.Providers.ZAICodingCN`, `LeanAgent.Models` | implemented |

## Utils

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `utils/abort-signals.ts` | `LeanAgent.AI.Util.Abort` | implemented | Polling abort signal state, combined-signal composition, abort-aware chunked sleep, throwIfAborted, abort-error detection (case-insensitive), and HTTP mid-transfer abort integration via flag file + XFERINFO exist with offline tests (`testAbort*`, `testHttpClientMidTransferAbort`). Progressive SSE event callbacks during transfer remain on HTTP transport partial. |
| `utils/diagnostics.ts` | `LeanAgent.AI.Util.Diagnostics`, `LeanAgent.Http` | implemented | Diagnostic error info, assistant message diagnostics, append helper, thrown-value formatting, provider error body extraction (`providerErrorInfoFromBody`/`providerHttpErrorMessage`), and response-header details helpers exist with offline tests (`testDiagnostics*`). Exact JS Error stacks are Exclusion List. Broader assistant-level live-stream diagnostic surfacing remains on protocol/HTTP rows. |
| `utils/estimate.ts` | `LeanAgent.AI.Util.Estimate` | implemented | Pi-style context/message/token estimation exists, including usage-anchor trailing-token logic, image estimate, UTF-16 text length behavior, and direct text-token estimate predictability coverage. |
| `utils/event-stream.ts` | `LeanAgent.AI.EventStream`, `LeanAgent.AI.Util.SSE`, `LeanAgent.Loop` | implemented | Buffered `AssistantMessageEventStream`, content/text/thinking/tool event reconstruction, legacy provider bridge, and Pi-style pushable `MutableAssistantMessageEventStream` (`createAssistantMessageEventStream`, push/end/result/toBuffered) exist with offline tests. True concurrent async-iterator waiters under live transport remain open (Exclusion-adjacent sequential settle). |
| `utils/hash.ts` | `LeanAgent.AI.Util.Hash` | implemented | Pi-compatible deterministic `shortHash`, including UTF-16 code-unit behavior, exists with golden tests. |
| `utils/headers.ts` | `LeanAgent.AI.Util.Headers`, `LeanAgent.Http`, `LeanAgent.AI.Api.OpenAICompletions`, `LeanAgent.AI.Api.OpenAIResponses` | implemented | Provider header filtering, case-insensitive merge/override behavior, custom request headers, response header parsing, and `onResponse` hook integration now have local runtime coverage across OpenAI-compatible, Responses, Azure OpenAI Responses, Anthropic, Google Generative AI, Google Vertex, Mistral, OpenAI Codex Responses, Bedrock, and OpenRouter Images. Remaining gaps are broader assistant-level diagnostic surfacing rather than transport/header plumbing itself. |
| `utils/json-parse.ts` | `LeanAgent.Json`, `LeanAgent.AI.Util.JsonParse` | implemented | Malformed string repair, parse-with-repair, empty streaming fallback, partial object/array closure, dangling comma cleanup, dangling object-field omission, partial-literal truncation, multi-pass completion (`tryHarderStreamingParse`), and always-object streaming parse exist with offline tests (`testJsonParse*`). Full `partial-json` package edge universe is not claimed. |
| `utils/node-http-proxy.ts` | `LeanAgent.AI.Util.Proxy` | implemented | Provider-scoped/ambient proxy env lookup, default port mapping, `NO_PROXY` host/port matching (including Pi Bedrock-style host exclusions), inferred proxy protocols, HTTP(S)-only validation, and URL-style resolved proxy normalization exist with offline tests (`testProxyEnvResolution`, `testProxyNoProxyMatching`, `testProxyRejectsUnsupportedProtocol`). Transport injection beyond libcurl remains on HTTP transport partial. |
| `utils/oauth/index.ts` | `LeanAgent.AI.OAuth` | implemented | OAuth provider registry (get/register/unregister/reset/list), built-in Anthropic/GitHub Copilot/OpenAI Codex providers, high-level refresh/getOAuthApiKey. Covered by offline OAuth registry tests under `testOAuth*` / compat OAuth bridges. |
| `utils/oauth/types.ts` | `LeanAgent.AI.OAuth.Types` | implemented | OAuth credentials, provider interface, login callbacks, device-code info, select prompts. Covered by offline OAuth type/login tests. |
| `utils/oauth/load.ts` | `LeanAgent.AI.OAuth`, `LeanAgent.AI.OAuth.Anthropic`, `LeanAgent.AI.OAuth.OpenAICodex`, `LeanAgent.AI.OAuth.GitHubCopilot` | implemented | Lean imports OAuth provider modules statically (no bundler graph); equivalent load-once lazy OAuth wiring lives in `Auth.lazyOAuth` / built-in OAuth registration. Covered by offline OAuth/bootstrap tests. |
| `utils/oauth/device-code.ts` | `LeanAgent.AI.OAuth` | implemented | Device-code polling supports `pending`, `slow_down`, `failed`, `complete`, default/minimum intervals, slow-down growth, deadline-capped sleeps, timeout messages, injectable cancellation/clock/sleep, and signal-driven mid-sleep cancellation with offline tests (`testOAuthDeviceCode*`). Provider-specific network login remains under OAuth provider rows. |
| `utils/oauth/github-copilot.ts` | `LeanAgent.AI.OAuth.GitHubCopilot` | implemented | Domain/base-URL helpers, device-code login/refresh, verification-URI validation, available-model discovery, OAuth `toAuth`, credential model filtering, and built-in registration exist with offline tests (`testGitHubCopilotOAuth*`). Browser-only UI automation is Exclusion List. |
| `utils/oauth/anthropic.ts` | `LeanAgent.AI.OAuth.Anthropic` | implemented | Token refresh, PKCE browser/manual-code login, localhost callback, state validation, callback/manual race, abortable manual-code, stored OAuth `toAuth`, and built-in registration exist with offline tests (`testAnthropicOAuth*`). Browser-only UI automation is Exclusion List. |
| `utils/oauth/openai-codex.ts` | `LeanAgent.AI.OAuth.OpenAICodex` | implemented | Token refresh, device-code start/poll/exchange, PKCE browser/manual-code login, localhost callback, state validation, abortable prompts, JWT account-id validation, and built-in registration exist with offline tests (`testOpenAICodexOAuth*`). Browser-only UI automation is Exclusion List. |
| `utils/oauth/oauth-page.ts` | `LeanAgent.AI.OAuth.Page` | implemented | Pi-style OAuth success/error callback HTML pages exist with escaping for all dynamic title, heading, message, and details fields, optional details rendering, and tests. |
| `utils/oauth/pkce.ts` | `LeanAgent.AI.OAuth.PKCE`, `native/http_client.c` | implemented | PKCE verifier generation uses OpenSSL `RAND_bytes`, S256 challenge generation uses OpenSSL `SHA256`, output is base64url without padding, and tests cover the RFC 7636 vector plus generated verifier/challenge shape. |
| `utils/overflow.ts` | `LeanAgent.AI.Util.Overflow` | implemented | Common provider overflow messages, separator-insensitive compact matching, non-overflow throttling/rate-limit/service-unavailable exclusions, silent usage overflow, and length-stop full-context heuristics match Pi `isContextOverflow` offline suite (`testOverflowClassifies*`, including Ollama/Together/LiteLLM/Poolside/Cerebras/Xiaomi cases). |
| `utils/provider-env.ts` | `LeanAgent.AI.Util.ProviderEnv` | implemented | Scoped provider env lookup, ambient env fallback, empty-value suppression, and merge semantics exist. Bun-specific `/proc/self/environ` fallback is not applicable to Lean. |
| `utils/retry.ts` | `LeanAgent.AI.Util.Retry` | implemented | Retryable assistant/provider error classification (Pi retryable/non-retryable pattern sets via contains+compact separator-insensitive matching), delay cap, and `withRetries` with abort-aware sleep exist. Offline Pi `retry.test.ts` matrix covered (`testRetryClassifiesAssistantErrors`, OpenAI/Bedrock explicit guidance, overloaded_error, quota exclusion). Cross-provider live request-matrix wiring remains on protocol rows. |
| `utils/sanitize-unicode.ts` | `LeanAgent.AI.Util.SanitizeUnicode` | implemented | UTF-16 surrogate pair preservation + unpaired high/low removal (`sanitizeSurrogateCodeUnits`) and Lean-string surrogate filtering (`sanitizeSurrogates`) with offline tests (`testSanitizeUnicodeSurrogates`). Wired into Anthropic/OpenAI/Google/Mistral/Bedrock/OpenRouter Images serialization paths; any remaining provider row gaps are tracked on those protocol rows. |
| `utils/typebox-helpers.ts` | `LeanAgent.AI.Schema` | implemented | Pi's provider-compatible `StringEnum` helper is exposed as `stringEnum`, including optional description/default fields. |
| `utils/validation.ts` | `LeanAgent.AI.Validation`, `LeanAgent.AI.Schema` | implemented | Tool lookup, argument validation, JSON Schema primitive coercion, required/object/array/enum/const, bounds, pattern subset, uniqueItems/contains, allOf/anyOf/oneOf/not, and formatted errors exist with offline tests (`testValidation*`). Full TypeBox/AJV universe is Exclusion List. |

## Images

| Pi source | Lean target | Status | Notes |
| --- | --- | --- | --- |
| `images.ts` | `LeanAgent.AI.Images` | implemented | `generateImages` / `generateImagesWithApi` through registered image API providers; OpenRouter built-in; abort-aware results (`testImages*`). |
| `images-models.ts` | `LeanAgent.AI.Images`, `LeanAgent.AI.Images.Models` | implemented | Runtime image model type, TS-style `getProvider`/`getModel` lookups alongside Lean `...?` helpers, image provider collection, provider factory, refresh, auth merge, and abort-aware error-as-result dispatch exist. Image providers now share Pi-style in-flight refresh dedupe, single-provider `model_source` failure wrapping, and best-effort refresh-all behavior. Shared OAuth credential resolution/refresh has image collection tests; live paid image provider matrix Exclusion. |
| `image-models.ts` | `LeanAgent.AI.Images.Models` | implemented | Static image model access exists for the current Pi image catalog. |
| `image-models.generated.ts` | `LeanAgent.AI.Images.Models` | implemented | Current generated OpenRouter image catalog is represented as checked-in Lean data with 37 models. |
| `images-api-registry.ts` | `LeanAgent.AI.Images.Registry` | implemented | Register/get/list/unregister/clear/reset image API dispatch exists with built-in replay and API mismatch wrapping. |
| `api/openrouter-images.ts` | `LeanAgent.AI.Api.OpenRouterImages` | implemented | OpenRouter Images Chat Completions payload construction, data-URL inputs, modalities, API-key auth, headers, payload/response hooks, retry, provider error extraction, abort preflight, and local HTTP generate tests exist (`testOpenRouterImages*`). Live paid provider matrix is Exclusion List. |
| `providers/openrouter-images.ts` | `LeanAgent.AI.Providers.OpenRouterImages` | implemented | Built-in OpenRouter image API registration, provider factory, OPENROUTER_API_KEY auth, and 37-model catalog exist with offline factory/catalog tests. |

## Test Mapping

Initial Lean parity should port tests in this order:

| Pi tests | Lean target | Status | Why first |
| --- | --- | --- | --- |
| `models-runtime.test.ts`, `providers.test.ts`, `supports-xhigh.test.ts`, `xhigh.test.ts` | model catalog and thinking tests | implemented | Protects provider/model registry, explicit OpenAI-compatible provider factories, OpenAI Responses catalog metadata, mixed OpenCode/OpenCode Go catalog/factory routing, dynamic refresh dedupe/failure semantics, cost calculation, thinking-level map helpers, auth-applied base URL overrides, and header-only auth dispatch. |
| `env-api-keys.test.ts`, `compat-env.test.ts`, Cloudflare auth tests | auth/env tests | implemented | Env API-key, stored credential precedence, file-backed API-key/OAuth credential roundtrip/integration, OAuth refresh/toAuth resolution, lazy OAuth load-once delegation, OAuth credential ownership without env fallback, typed `ModelsError(auth|oauth)` wrapping for read/modify/resolve/refresh/`toAuth` failures, OAuth provider registry/high-level helper behavior, device-code polling behavior, callback page rendering/escaping, PKCE verifier/challenge behavior, GitHub Copilot OAuth helper parsing/model rewrite plus local device-code login/refresh, login-signal cancellation, verification-URI handling, and built-in-model collection filtering/base-URL rewriting from stored OAuth metadata, Anthropic OAuth refresh plus local browser/manual-code, localhost callback, abortable manual-code prompt, and state-validation coverage, OpenAI Codex OAuth refresh plus local browser/manual-code, localhost callback, abortable manual-code prompt, login-signal cancellation, and device-code login coverage, full provider env map including OpenCode/OpenCode Go shared `OPENCODE_API_KEY`, Anthropic OAuth-token precedence, Bedrock/Vertex ambient markers, Cloudflare Workers AI / AI Gateway auth resolution and factory wiring, compat registry dispatch, request API key pass-through, known-provider env key injection, catalog-outside provider env injection, and legacy alias registry dispatch are covered. Built-in OAuth login providers (Anthropic/GitHub Copilot/OpenAI Codex) covered offline; browser-only UI automation Exclusion. |
| `stream.test.ts`, `empty.test.ts`, `abort.test.ts` | event stream tests | implemented | Stream result, text/thinking/tool events, partial snapshots, tool delta payloads, empty-content completion, and immediate abort results for collection/compat/Faux dispatch are covered in Lean. Async iterator/backpressure is Exclusion-adjacent sequential settle; mid-transfer abort and progressive streams covered offline; live timing Exclusion. |
| `openai-completions-*.test.ts` | OpenAI completions tests | implemented | Payload tests cover empty tools, tool history, tool choice, max tokens, temperature, reasoning effort, thinking-level payload aliases, prompt cache key/retention/clamp helper behavior, session-affinity headers with caller override, direct context-aware user-image serialization, grouped tool-result image replay, thinking-as-text replay including thinking-only serialization and runtime dispatch, encrypted `reasoning_details` binding/replay, Anthropic-style cache markers, strict-mode suppression, Cloudflare AI Gateway `/compat` conservative OpenAI-compatible fields plus BYOK auth/header preservation and session-affinity runtime headers, built-in z.ai `tool_stream` gating plus explicit compat override and no-tools omission, chat-template omission for non-reasoning models, explicit `mapped := none` suppression for compat reasoning payloads, routed `responseModel` capture for OpenAI-compatible router ids, reasoning-model `developer` versus `system` instruction-role selection, Groq Qwen3 clamp-to-default `reasoning_effort`, built-in z.ai GLM-5.2 reasoning-effort mappings plus thinking-off omission, OpenCode Go assistant replay normalization from `reasoning` to `reasoning_content` while generic providers preserve `reasoning`, null chunk tolerance, Pi-style unknown `finish_reason` error mapping, missing `finish_reason` termination errors, stable-index and id-keyed mixed parallel `tool_calls` coalescing, chunk-level and choice-level `cache_write_tokens` streaming usage parsing plus no-double-count reasoning-token totals, streaming payload/SSE parsing, buffered streaming runtime dispatch, typed legacy alias dispatch, request/response headers, usage parsing, provider HTTP diagnostics, repaired tool arguments, and legacy assistant tool-call omission. Network provider matrix and true live-stream timing tests are missing. |
| `retry.test.ts`, `diagnostics.test.ts`, `estimate.test.ts`, `overflow.test.ts`, `validation.test.ts`, `unicode-surrogate.test.ts` | util tests | implemented | Retry classifier/policy, abort helper combination, retry-delay abort cancellation, diagnostics extraction/round-trip, estimate utilities including text-token predictability, provider header filtering/merge, proxy env resolution, JSON repair/streaming fallback, JSON Schema validation/coercion including string pattern cases, Unicode surrogate sanitization helpers, and Anthropic/OpenAI Completions serialization coverage are covered alongside overflow detection and OpenAI transient HTTP retry. Live provider unicode-surrogate tests are missing. |
| `faux-provider.test.ts` | faux provider tests | implemented | Queued/factory responses, helper blocks, multi-model factories, usage/cache estimates, model rewrite, collection dispatch, compat register/unregister, chunked deltas (`tokenSize`), factory throw, preflight + mid-stream paced abort (`tokensPerSecond`) covered by `testFauxProvider*` / `testCompatRegisterFauxProvider*`. Exact JS microtask scheduling is Exclusion-adjacent sequential settle. |
| `session-resources.test.ts` | session resource cleanup tests | implemented | Cleanup registration, unregister handles, session id propagation, continued cleanup after failures, and aggregate errors are covered. |
| `images*.test.ts`, `openrouter-images.test.ts` | image tests | implemented | Generic image API registry, built-in replay, image provider collection CRUD/refresh/auth merge/error result, shared OAuth credential resolution/refresh, collection abort preflight, OpenRouter provider factory auth, full current image catalog lookup/count, OpenRouter Images payload construction, direct abort-result handling, local HTTP dispatch, headers, response hooks, output parsing, usage/cost parsing, and missing-key errors are covered. Live paid provider matrix is Exclusion List. |
| OpenAI/Anthropic/Google/Mistral/Bedrock/Azure/Codex tests | provider protocol tests | implemented | OpenAI Responses local provider catalog/factory dispatch/typed legacy alias tests, OpenAI Responses shared foreign-toolcall/fallback-message-id tests, OpenAI Responses terminal-event parse/early-EOF compat error-stream tests, OpenAI Codex payload/header/helper/local SSE/provider catalog/stored OAuth/compat dispatch/typed legacy alias tests plus provider HTTP error/response-header diagnostics, local OAuth refresh, browser/manual-code login, localhost callback login, device-code login, and helpful 404 device-auth coverage, GitHub Copilot dynamic-header helper tests plus provider catalog/factory/env-key/stored-OAuth/builtin aggregation coverage, Anthropic Messages payload/header/session-affinity/prompt-cache/response-parser/SSE parser/local streaming/typed legacy alias/provider catalog tests plus local OAuth refresh, browser/manual-code login, localhost callback login, and state-mismatch coverage, Anthropic-compatible Kimi Coding/MiniMax/MiniMax CN/Vercel catalog/factory/env tests, mixed OpenCode/OpenCode Go catalog/factory/API-route/env tests, Google Generative AI payload/response-parser/SSE parser/local streaming/typed legacy alias/provider catalog tests, Google shared convertTools schema field/sanitization tests, Google shared thinking/image-routing/tool-call thought-signature tests, Google Vertex URL/header/auth/local streaming/typed legacy alias/provider catalog tests including service-account and authorized-user ADC token acquisition, Mistral Conversations payload/header/tool-choice/reasoning/prompt-cache/cache-retention/response-parser/SSE parser/local streaming/typed legacy alias/provider catalog tests, Bedrock payload/helper/catalog/auth/typed legacy alias plus buffered EventStream runtime, GovCloud thinking-display, SigV4 request-signing, and local runtime/compat coverage, Azure OpenAI Responses base URL, deployment mapping, payload, local streaming transport, typed legacy alias, provider catalog/factory, env auth, builtin aggregation, and compat built-in dispatch are covered. Codex WebSocket parity and full browser-automation parity are still missing. Bedrock progressive event-stream offline covered; Codex WebSocket and live browser OAuth remain Exclusion. |

## Milestone completion (historical M1–M6)

**Offline package inventory status: CLOSED** (tables above: no `missing`/`partial` status cells; every Pi `packages/ai/src/**/*.ts` file is scoped).

| Milestone | Offline status | Notes |
| --- | --- | --- |
| M1 Core AI Types | **done** | Types, serialization tests, legacy Core wrappers |
| M2 Event Stream | **done** | Buffered + progressive streams; concurrent async iterators Exclusion-adjacent |
| M3 Models + Auth | **done** | Collection, providers, OAuth offline (browser UI Exclusion) |
| M4 OpenAI-compatible family | **done** | Completions progressive SSE + pin catalogs; live paid matrix Exclusion |
| M5 Protocol expansion | **done** offline | Anthropic/Google/Vertex/Mistral/Azure/Responses/Codex/Bedrock progressive paths offline; live AWS SDK ambient chain / Codex WebSocket Exclusion-adjacent |
| M6 Images + Compat | **done** | Registry, OpenRouter images, compat barrel |

Residual **Exclusion List** items (not ledger `partial` rows): live paid multi-provider matrices, browser-only OAuth automation, exact concurrent async-iterator races, full AWS SDK default credential chain beyond ambient/profile helpers.

## Rules for Updating This Ledger

- Every AI change must update this file in the same commit when status changes.
- New Lean modules must cite the Pi source row they are closing in commit notes or PR text.
- A row can become `implemented` only with tests or an explicit reason why runtime validation is impossible.
- `vendor/pi` is read-only. Do not edit reference files.
- Prefer behavior parity over line-by-line TypeScript translation.
